---
date: 2026-08-06
researcher: GPT-5.6 Luna
git_commit: 70d862d241e752dea12449f439b8e2de384a08e2
branch: main
repository: atomic-monorepo
topic: Pi v0.83.0..v0.84.0 upstream commit and file port matrix
tags: [implementation, evidence, pi-0.84.0, port-matrix]
status: complete
last_updated: 2026-08-06
last_updated_by: GPT-5.6 Luna
breaking_changes_allowed: false
compatibility_context: Preserve Atomic public SDK, branding, CLI, paths, legacy PI_*/.pi aliases, providers, isolated runtime, Verbatim Compaction, and versionless manifests.
---

# Pi v0.84.0 Port Matrix

This matrix classifies **every one of the 341 commits** in upstream `v0.83.0..v0.84.0` against Atomic's source and dependency boundaries. Each row carries the upstream short SHA, subject, one classification, and the shard's evidence for the outcome.

## Source and counting basis

- Commit inventory (341 rows): [`research/upstream-v0.83.0-v0.84.0-commits.txt:1-341`](upstream-v0.83.0-v0.84.0-commits.txt).
- Per-commit changed paths: [`research/upstream-v0.83.0-v0.84.0-commit-paths.txt:1-2596`](upstream-v0.83.0-v0.84.0-commit-paths.txt).
- File inventory: [`research/upstream-v0.83.0-v0.84.0-name-status.txt:1-593`](upstream-v0.83.0-v0.84.0-name-status.txt).
- Range summary: [`research/upstream-v0.83.0-v0.84.0-log-stat.txt:1-4149`](upstream-v0.83.0-v0.84.0-log-stat.txt).
- Full upstream coding-agent patch: **not committed** (622 KB, exceeds the repository's added-file guard). Rows citing `…coding-agent.diff:NNNN` refer to this patch; regenerate it with `git diff v0.83.0 v0.84.0 -- packages/coding-agent > upstream-v0.83.0-v0.84.0-coding-agent.diff` in a checkout of `earendil-works/pi` to resolve those line numbers.

The range contains 341 commits, of which 107 touch `packages/coding-agent`. The five recorded evidence files above are the counting basis; the 17 shard fragments contribute one row per inventory SHA. The join found 341 unique shard prefixes, with no missing, extra, duplicate, or contradictory row.

## Classification vocabulary

Exactly one classification applies to each commit:

| Classification | Meaning |
| --- | --- |
| **ported** | Upstream behaviour now present in Atomic-owned source, adapted only where Atomic's file split requires it. |
| **intentionally adapted** | Upstream behaviour is present, but the mechanism differs because Atomic's architecture differs. The difference is deliberate and stated. |
| **inherited dependency** | The change lives in `packages/ai`, `packages/tui`, or `packages/agent`, which Atomic consumes as an exact-resolved `@earendil-works/pi-*` dependency. Atomic vendors no copy. |
| **equivalent** | Atomic already had the behaviour before this range; nothing to do. |
| **not applicable** | No Atomic counterpart exists: upstream release stamps, upstream-only packages, repository governance, upstream test-harness maintenance, or bookkeeping commits. |

## Commit matrix (341/341)

| # | Commit | Subject | Classification | Evidence |
|---:|---|---|---|---|
| 1 | `a5f43bf` | Release v0.84.0 | **not applicable** | Upstream release stamping spans manifests, locks, changelogs, examples, and generated pi-ai metadata (`…commit-paths.txt:1-35`; `…log-stat.txt:7-40`). Atomic keeps versionless manifests at `packages/coding-agent/package.json:1-4` and `npm-shrinkwrap.json:1-16`; dependency metadata belongs to the separate reconciliation. |
| 2 | `d6442dd` | fix: avoid registry resolution during version bumps | **equivalent** | Upstream changes root release scripts (`…commit-paths.txt:36-39`); Atomic stamps the lock directly without `npm version` (`scripts/bump-version.ts:190-201`), covered by `test/unit/bump-version-script.test.ts` › `updates every versioned package manifest and README badge` (`:166-236`). |
| 3 | `6d0455f` | docs: audit unreleased changelogs | **not applicable** | Six upstream `CHANGELOG.md` files only (`…commit-paths.txt:40-47`; `…log-stat.txt:58-64`). Atomic owns its own unreleased history at `packages/coding-agent/CHANGELOG.md:1-12`; no runtime behavior changed. |
| 4 | `5cd46ee` | docs(agent): simplify harness work package workflow | **not applicable** | Upstream changes `packages/agent/docs/{harness-v2.md,harness-v2-test-matrix.md}` only (`…commit-paths.txt:48-51`); the consumed agent package ships only `dist` and README (`node_modules/@earendil-works/pi-agent-core/package.json:19-22`). |
| 5 | `6189e53` | fix: replace session summaries with durable metadata (#7708) | **not applicable** | Upstream narrows the new remote-client `RemoteSession.sessions` getter to `SessionMetadata` (`…commit-paths.txt:52-70`; `…coding-agent.diff:2471,2575-2577`) alongside upstream-only client, protocol, and server packages. Atomic has no `src/client` or pi-client/pi-protocol dependency (`packages/coding-agent/package.json:80-85`); its local JSONL `SessionInfo` catalog is separate (`src/core/session-manager-types.ts:200-220`, `session-manager-list.ts:208-256`). |
| 6 | `29ad292` | fix(agent): simplify jsonl append failure test | **not applicable** | Upstream changes only `packages/agent/test/harness/session/jsonl.test.ts` (`…commit-paths.txt:71-73`), an unpublished harness test (`node_modules/@earendil-works/pi-agent-core/package.json:19-22`). |
| 7 | `a838c06` | harness-v2 jsonl session atomic writes for forks & torn-tail truncation (#7707) | **inherited dependency** | Upstream changes crash-safe JSONL repo, storage, state, and Node environment code under `packages/agent/src` (`…commit-paths.txt:74-85`); Atomic consumes, rather than vendors, `@earendil-works/pi-agent-core` (`packages/coding-agent/package.json:83`, `package-lock.json:778-793`). |
| 8 | `fadb74f` | docs(agent): separate harness design from implementation status | **not applicable** | Upstream changes `packages/agent/docs/harness-v2.md` only (`…commit-paths.txt:86-88`); that design document is outside the shipped agent artifact (`node_modules/@earendil-works/pi-agent-core/package.json:19-22`). |
| 9 | `35f5c26` | feat: add telemetry reference adapter and span composition | **inherited dependency** | The implementation is in upstream `packages/agent` and new `packages/telemetry`; coding-agent changes only changelog copy (`…commit-paths.txt:89-110`). Atomic vendors neither package and consumes pi runtime packages externally (`packages/coding-agent/package.json:80-85`); pi-agent-core/pi-ai 0.84 reconciliation must bring the pi-telemetry leaf now absent from `package-lock.json:778-829`. |
| 10 | `b9497c8` | fix(ai): correct Fireworks GLM prompt caching, closes #7676 | **inherited dependency** | Upstream fixes the pi-ai Fireworks model generator and tests (`…commit-paths.txt:111-116`); Atomic loads the builtin provider catalog from `@earendil-works/pi-ai` (`packages/coding-agent/src/core/model-runtime-providers.ts:2`). |
| 11 | `7f63f37` | docs: add missed JSONL matrix rows | **not applicable** | Upstream changes only `packages/agent/docs/harness-v2-test-matrix.md` (`…commit-paths.txt:117-119`); agent documentation is not shipped to Atomic (`node_modules/@earendil-works/pi-agent-core/package.json:19-22`). |
| 12 | `77f4840` | chore: qa2 tests (#7706) | **not applicable** | Upstream changes harness docs and tests under `packages/agent` and `packages/session-backends` only (`…commit-paths.txt:120-126`); Atomic declares no session-backends package (`packages/coding-agent/package.json:80-113`). |
| 13 | `9859eaa` | docs: reserve QA2 | **not applicable** | Upstream changes the harness work-status document only (`…commit-paths.txt:127-129`); Atomic does not ship upstream agent docs (`node_modules/@earendil-works/pi-agent-core/package.json:19-22`). |
| 14 | `2920265` | docs: update harness v2 md on how to use test matrix | **not applicable** | Upstream changes `packages/agent/docs/harness-v2.md` only (`…commit-paths.txt:130-132`); it is outside Atomic’s consumed agent artifact (`node_modules/@earendil-works/pi-agent-core/package.json:19-22`). |
| 15 | `95bfbdc` | docs: mark QA1 done | **not applicable** | Upstream changes a harness work-status marker only (`…commit-paths.txt:133-135`); no shipped Atomic behavior is involved (`node_modules/@earendil-works/pi-agent-core/package.json:19-22`). |
| 16 | `22db569` | docs: harness v2 test matrix | **not applicable** | Upstream adds `packages/agent/docs/harness-v2-test-matrix.md` only (`…commit-paths.txt:136-138`); Atomic’s port evidence is separately maintained under `research/` (`research/pi-0.83.0-port-matrix.md:16-21`). |
| 17 | `8ed2325` | docs: reserve QA1 | **not applicable** | Upstream changes only `packages/agent/docs/harness-v2.md` (`…commit-paths.txt:139-141`); the consumed package contains no harness documentation (`node_modules/@earendil-works/pi-agent-core/package.json:19-22`). |
| 18 | `bbbc73b` | chore: approve contributors from issue #7687 | **not applicable** | Upstream changes `.github/APPROVED_CONTRIBUTORS` only (`…commit-paths.txt:142-144`); Atomic maintains separate contributor policy (`CONTRIBUTING.md:27-35`). |
| 19 | `ffa40c3` | chore: approve contributors from issue #7683 | **not applicable** | Upstream changes `.github/APPROVED_CONTRIBUTORS` only (`…commit-paths.txt:145-147`); Atomic’s contributor process is independent (`CONTRIBUTING.md:27-35`). |
| 20 | `fb92f13` | chore: approve contributors from issue #7668 | **not applicable** | Upstream changes `.github/APPROVED_CONTRIBUTORS` only (`…commit-paths.txt:148-150`); Atomic’s contributor process is independent (`CONTRIBUTING.md:27-35`). |
| 21 | `6282221` | fix(agent): scope JSONL session IDs to working directories | **inherited dependency** | Upstream changes the agent harness JSONL repository and its test (`…commit-paths.txt:151-154`); Atomic takes that package from `@earendil-works/pi-agent-core` (`packages/coding-agent/package.json:83`, `package-lock.json:778-793`). |
| 22 | `69bab86` | docs(agent): reserve harness v2 R3 | **not applicable** | Upstream changes only `packages/agent/docs/harness-v2.md` (`…commit-paths.txt:155-157`); that work-package record is not a shipped dependency surface (`node_modules/@earendil-works/pi-agent-core/package.json:19-22`). |
| 23 | `c0947e6` | fix(ai): remove stale adaptive thinking model expectation | **not applicable** | Upstream changes only `packages/ai/test/anthropic-adaptive-thinking-models.test.ts` (`…commit-paths.txt:158-160`), which is excluded from the pi-ai package (`node_modules/@earendil-works/pi-ai/package.json:46-49`). |
| 24 | `b0bd0ff` | fix(coding-agent): only label known subscriptions in footer | **intentionally adapted** | Upstream adds `isSubscription`, `isUsingSubscription()`, and the footer predicate (`…commit-paths.txt:161-183`; `…coding-agent.diff:4523-4525,6114-6116`). Atomic still labels every OAuth login at `src/modes/interactive/components/footer.ts:100-107` and exposes only `isUsingOAuth` at `src/core/model-runtime.ts:305-307`. Port the marker through `core/extensions/provider-types.ts:37-52` and `provider-composer-internal.ts:28-36,225-246`, add the runtime predicate and footer switch, then test subscription versus generic OAuth. |
| 25 | `6b461b7` | feat: extract telemetry package | **inherited dependency** | Upstream moves telemetry from pi-ai into `packages/telemetry` and refactors pi-agent-core; its coding-agent changes are upstream build/shrinkwrap wiring (`…commit-paths.txt:184-223`; `…log-stat.txt:368-405`). Atomic has its own binary build path (`packages/coding-agent/package.json:68-73`) and no telemetry workspace; the new package arrives through pi dependency reconciliation, not an Atomic source copy. |
| 26 | `db48124` | docs: audit unreleased changelogs | **not applicable** | Four upstream changelogs only (`…commit-paths.txt:224-229`; `…log-stat.txt:414-418`). Atomic’s changelog is independently maintained under `## [Unreleased]` (`packages/coding-agent/CHANGELOG.md:1-12`), with released-section immutability enforced at `test/unit/changelog.test.ts:200-251`. |
| 27 | `639a466` | fix(ai): update Anthropic abort test model | **not applicable** | Upstream changes only `packages/ai/test/abort.test.ts` (`…commit-paths.txt:230-232`); Atomic’s abort behavior is independently exercised by `manual compaction re-entrancy > does not overwrite the live abort controller, so abortCompaction() cancels both callers` (`packages/coding-agent/test/compaction-reentrancy.test.ts:89,123-151`), not this upstream fixture. |
| 28 | `5446cd7` | feat(coding-agent): rename UI mode to TUI mode | **intentionally adapted** | Upstream renames the fullscreen-renderer setting, CLI flag, selector, and switch path (`…commit-paths.txt:233-250`; `…coding-agent.diff:1790-1804,5597,5656-5662,6603-6614`). Atomic has no renderer-mode setting (`src/core/settings-types.ts:115-144`), parses only app output `--mode` (`src/cli/args.ts:105-109,205-228`), and creates one `TUI` (`src/modes/interactive/interactive-mode-base.ts:312-329`). Port the feature through the isolated interactive engine using final `tuiMode` and `--tui-mode` names, never the obsolete UI names. |
| 29 | `2c819a4` | docs(agent): align telemetry design with implementation | **not applicable** | Upstream changes only `packages/agent/docs/harness-v2.md` (`…commit-paths.txt:251-253`); the document is not shipped by `pi-agent-core` (`node_modules/@earendil-works/pi-agent-core/package.json:19-22`). |
| 30 | `d07889d` | feat(ai): support thinking_token_budget on openai-completions (#7638) | **inherited dependency** | Upstream adds OpenAI-completions serialization and compat support in pi-ai (`…commit-paths.txt:254-261`); Atomic already passes `thinkingBudgets` into the upstream Agent (`packages/coding-agent/src/core/sdk.ts:349`) and preserves compat overlays generically (`core/provider-composer-internal.ts:73-92`). |
| 31 | `6ca4234` | event bus leak fix (#7656) | **intentionally adapted** | Upstream tracks extension event subscriptions and invalidates the outgoing runner on reload (`…commit-paths.txt:262-268`; `…coding-agent.diff:3341-3344,3760-3808,3951-3952`). Atomic’s split runtime has no tracker (`src/core/extensions/loader-runtime.ts:212-217`), exposes raw `events: eventBus` (`loader-api.ts:238`), and rebuilds after reload without invalidating the old runner (`agent-session-extension-bindings.ts:270-283`). Port tracked unsubscribe wrappers, old-runner invalidation before reload, and the lifecycle regression test. |
| 32 | `7e45ddc` | fix: recognize comma after lgtm in approval comments (fixes #7399) (#7663) | **not applicable** | Upstream changes `.github/workflows/approve-contributor.yml` only (`…commit-paths.txt:269-271`); Atomic has its own release workflow (`.github/workflows/publish.yml:1-31`). |
| 33 | `2bb7ba4` | feat: harness v2 r2 (#7669) | **inherited dependency** | Upstream changes the durable harness reducer and its tests (`…commit-paths.txt:272-275`); `packages/agent` is supplied by Atomic’s external `pi-agent-core` dependency (`package-lock.json:778-793`). |
| 34 | `2f7f75a` | fix(ai): replace qwen3.8-max-preview with qwen3.8-max on token plan (#7670) | **inherited dependency** | Upstream updates pi-ai’s generated Qwen token-plan catalog (`…commit-paths.txt:276-279`); Atomic takes builtin models from pi-ai (`packages/coding-agent/src/core/model-runtime.ts:108-115`) and owns no `qwen3.8` override. |
| 35 | `14cc26e` | fix(ai): restore Copilot models from account policy (#7672) | **inherited dependency** | Upstream changes pi-ai’s GitHub Copilot OAuth model-policy fallback (`…commit-paths.txt:280-283`); Atomic’s Copilot wrapper explicitly leaves OAuth behavior upstream (`packages/coding-agent/src/core/copilot-env-routing.ts:95-103`). |
| 36 | `04d6447` | feat: add typed telemetry contracts | **inherited dependency** | Upstream adds telemetry contracts and request context across `packages/{agent,ai}` (`…commit-paths.txt:284-303`); Atomic’s own telemetry only gates install reporting (`packages/coding-agent/src/core/telemetry.ts:9-13`), while the new contracts remain pi-agent-core/pi-ai owned (`package-lock.json:778-818`). |
| 37 | `d4eaf05` | fix(coding-agent): support path globs on Windows | **intentionally adapted** | Upstream rewrites `/` to `[/\\]` for fd full-path globs on Windows (`…commit-paths.txt:304-307`; `…coding-agent.diff:5735-5742`). Atomic’s fd fallback builds `fdPattern` without that guard (`packages/coding-agent/src/core/tools/find.ts:660-670`); native search normalizes paths separately (`crates/atomic-natives/src/glob_util.rs:8-37`, `fs_cache/paths.rs:34-43`) but fd remains reachable at `find.ts:637`. Port the guarded rewrite after the `**/` prefix and add a test that forces the fd fallback. |
| 38 | `05e89b4` | feat(tui): render LaTeX math in Markdown | **inherited dependency** | Upstream adds pi-tui LaTeX rendering and Markdown support (`…commit-paths.txt:308-315`); Atomic renders assistant text through pi-tui `Markdown` (`packages/coding-agent/src/modes/interactive/components/assistant-message.ts:2`). |
| 39 | `16ad96a` | feat(tui): add configurable prompt history actions | **intentionally adapted** | Upstream adds TUI history actions plus the coding-agent precedence guard (`…coding-agent.diff:6095-6105`). Atomic still resolves `pi-tui@0.83.0` (`package-lock.json:819-822`), spreads its bindings (`src/core/keybindings.ts:64-65`), maps `ctrl+p` to model cycling (`:77-80`), then checks app handlers before the base editor (`components/custom-editor.ts:172-193`). Port the TUI bump, guard, docs (`docs/keybindings.md:29-40,176-213`), and regression test. |
| 40 | `229afb8` | fix(tui): skip OSC 8 scan for plain prefixes (#7665) | **inherited dependency** | Upstream changes only `packages/tui/src/utils.ts` (`…commit-paths.txt:327-329`); Atomic imports its width helpers from pi-tui (`packages/coding-agent/src/modes/interactive/components/chat-session-host-rendering.ts:1`). |
| 41 | `8ecf8a9` | feat(coding-agent): support AGENTS.override.md (#7681) | **intentionally adapted** | Upstream prepends the override in context discovery and compact-read classification (`…coding-agent.diff:5484-5485,5770-5771`). Atomic omits it from `resource-loader-context-files.ts:40`, `core/tools/read.ts:89`, and its separate trust-input scan (`core/trust-manager.ts:226`); preserve Atomic’s project-trust cutoff (`resource-loader-context-files.ts:151-153`) while adding override precedence to all three seams, docs, and loader/read tests. |
| 42 | `3852cb2` | fix(coding-agent): send prompts queued during compaction | **intentionally adapted** | Upstream clears manual compaction state before `compaction_end` (`…coding-agent.diff:3236-3246`). Atomic’s Verbatim Compaction flow emits at `core/agent-session-compaction.ts:257,262-269`, while its controller is cleared and agent reconnected only in `:300-305`; synchronous `_emit` (`agent-session-events.ts:35-38`) therefore exposes `isCompacting` as true (`agent-session-accessors.ts:45-52`) to the existing queue flush (`interactive-agent-events.ts:401`). Hand-place the idle transition before the manual event without breaking Atomic’s reconnect/engine lifecycle, and add a listener-queued-prompt test. |
| 43 | `459eb5e` | docs(agent): finalize harness telemetry design | **not applicable** | Upstream changes only `packages/agent/docs/harness-v2.md` (`…commit-paths.txt:345-347`); it is outside the pi-agent-core package payload (`node_modules/@earendil-works/pi-agent-core/package.json:19-22`). |
| 44 | `4254e0d` | fix(tui): include Windows native rebuild sources | **inherited dependency** | Upstream changes pi-tui package payload rules for Windows rebuild sources (`…commit-paths.txt:348-351`); Atomic’s binary copy takes only existing `prebuilds/win32-*` assets (`packages/coding-agent/package.json:70`), so the source-package change arrives through pi-tui. |
| 45 | `29d9f08` | fix(tui): avoid throttling keyboard input | **inherited dependency** | Upstream changes pi-tui’s input/render loop and test (`…commit-paths.txt:352-356`); Atomic constructs the dependency’s `TUI` directly (`packages/coding-agent/src/modes/interactive/interactive-mode-base.ts:324-329`). |
| 46 | `9524d3a` | fix(coding-agent): normalize Windows shell drive paths | **intentionally adapted** | Upstream adds Git Bash/MSYS/Cygwin/WSL drive conversion before tilde expansion (`…coding-agent.diff:8250-8268`). Atomic has no helper; `normalizePath()` passes directly from `stripAtPrefix` (`src/utils/paths.ts:72-74`) to Atomic’s `getHomeDir()` expansion (`:76-82`), which `resolvePath()` consumes (`:92-95`). Add the normalizer at that seam and upstream-equivalent Windows conversion/passthrough coverage in `test/paths.test.ts`. |
| 47 | `17f7204` | fix(agent): validate required JSONL mutation fields | **inherited dependency** | Upstream changes the agent harness JSONL codec plus tests (`…commit-paths.txt:362-366`); codec behavior belongs to Atomic’s external `pi-agent-core` dependency (`packages/coding-agent/package.json:83`, `package-lock.json:778-793`). |
| 48 | `104364f` | docs(agent): mark JSONL J2 complete | **not applicable** | Upstream changes only a harness work-status line (`…commit-paths.txt:367-369`); no shipped Atomic behavior changes (`node_modules/@earendil-works/pi-agent-core/package.json:19-22`). |
| 49 | `e61d8f4` | docs(agent): mark JSONL J1 complete | **not applicable** | Upstream changes only a harness work-status line (`…commit-paths.txt:370-372`); no shipped Atomic behavior changes (`node_modules/@earendil-works/pi-agent-core/package.json:19-22`). |
| 50 | `acb2fd9` | fix(agent): complete JSONL storage round-trip coverage | **not applicable** | Upstream adds only `packages/agent/test/harness/session/jsonl-storage.test.ts` (`…commit-paths.txt:373-375`), an unpublished upstream test file (`node_modules/@earendil-works/pi-agent-core/package.json:19-22`). |
| 51 | `d3d2c2e` | chore: approve contributors from issue #7642 | **not applicable** | Upstream changes `.github/APPROVED_CONTRIBUTORS` only (`…commit-paths.txt:376-378`); Atomic governs contributors through its own policy (`CONTRIBUTING.md:27-35`). |
| 52 | `48b8b54` | fix(agent): require JSONL session cwd at creation | **inherited dependency** | Upstream changes agent JSONL repository/types and tests (`…commit-paths.txt:379-383`); Atomic consumes the package externally (`packages/coding-agent/package.json:83`, `package-lock.json:778-793`). |
| 53 | `c7fc779` | feat(agent): complete JSONL codec contract coverage | **not applicable** | Upstream changes only harness documentation and codec tests (`…commit-paths.txt:384-387`); neither is shipped in the consumed pi-agent-core artifact (`node_modules/@earendil-works/pi-agent-core/package.json:19-22`). |
| 54 | `2f550d8` | fix: project open operation id to lanes (#7654) | **inherited dependency** | Upstream changes agent JSONL/memory storage alongside an upstream-only SQLite backend (`…commit-paths.txt:388-398`); the agent half is inherited through `pi-agent-core` (`package-lock.json:778-793`) and Atomic vendors no session-backends source. |
| 55 | `b3ec7c0` | fix(agent): stop JSONL repositories from retaining sessions | **inherited dependency** | Upstream changes `packages/agent/src/harness/session/jsonl/repo.ts` and its tests (`…commit-paths.txt:399-403`); that repository remains pi-agent-core-owned (`packages/coding-agent/package.json:83`). |
| 56 | `ce5d98c` | fix(coding-agent): keep unit model stores in memory | **intentionally adapted** | Upstream’s test helper injects `InMemoryCodingAgentModelsStore` while retaining optional `models.json` (`…coding-agent.diff:12195-12205`). Atomic has an in-memory-only helper (`test/model-runtime-test-utils.ts:17-19`) and modern harness (`test/suite/harness.ts:119-127`), but generic `createModelRegistry()` omits `modelsStore` (`:13-15`), so `ModelRuntime.create()` selects `FileModelsStore` for default or supplied paths (`src/core/model-runtime.ts:100-107`); active callers include `test/extensions-input-event.test.ts:33` and `test/sdk-openrouter-attribution.test.ts:99-101`. Port helper injection and affected test fixtures. |
| 57 | `4d68d93` | fix(coding-agent): shorten file lock retry delays | **intentionally adapted** | Upstream replaces long lockfile retries with bounded `ELOCKED`-only 10 ms exponential retries (`…coding-agent.diff:3415-3454`). Atomic’s async file backend still delegates the old `100 ms`–`10 s` retry policy to `proper-lockfile` (`src/core/auth-storage-backends.ts:223-236`). Port the bounded schedule into this split backend, retain Atomic’s distinct layered-delete budget (`:14-20`) and synchronous lock path (`:66-91`), and add the briefly contended `ELOCKED` regression case. |
| 58 | `2c79ce4` | fix(coding-agent): avoid custom store lock convoys | **intentionally adapted** | Upstream extends revision/read state to custom auth and model-store paths rather than leaving non-shared paths on locking reads (`…coding-agent.diff:4839-4843`). Atomic deliberately solves auth reads through lock-free atomic snapshots (`src/core/auth-storage-backends.ts:22-40,93-132`), but `FileModelsStore` has no path/read state and locks every read (`src/core/models-store.ts:25-40`). Adapt the custom catalog-path half with the `7cf90c1` cache or Atomic’s lock-free backend, while preserving layered auth semantics. |
| 59 | `a79b226` | fix(agent): match coding-agent JSONL session layout | **inherited dependency** | Upstream changes the agent harness JSONL layout (`…commit-paths.txt:423-428`); Atomic’s separate SessionManager layout is at `session-manager-paths.ts:10-15` and `session-manager-entries.ts:60-63`, while the harness change remains external in `pi-agent-core` (`package-lock.json:778-793`). |
| 60 | `7a10328` | docs: f0 done | **not applicable** | Upstream changes only `packages/agent/docs/harness-v2.md` (`…commit-paths.txt:429-431`); it is upstream work-package bookkeeping, not a shipped Atomic surface. |
| 61 | `ed303b9` | chore: scaffold test | **not applicable** | Upstream adds only `packages/agent/test/harness/agent-harness-scaffold.test.ts` (`…commit-paths.txt:432-434`); pi-agent-core publishes `dist` and README, not tests (`node_modules/@earendil-works/pi-agent-core/package.json:19-22`). |
| 62 | `14c89ad` | chore: UnavailableRegistry | **inherited dependency** | Upstream changes `packages/agent/src/harness/agent-harness.ts` plus docs and tests (`…commit-paths.txt:435-439`); the shipped harness API is provided by Atomic’s external `pi-agent-core` dependency (`packages/coding-agent/package.json:83`, `package-lock.json:778-793`). |
| 63 | `abac29f` | docs(agent): reserve harness R2 work package | **not applicable** | Only upstream `packages/agent/docs/harness-v2.md` changes (`…commit-paths.txt:440-442`); this work-package note is not a pi-agent runtime artifact, while Atomic consumes only the runtime package (`packages/coding-agent/package.json:83`). |
| 64 | `c518004` | docs(agent): clarify lane reduction contract (#7662) | **not applicable** | The delta is solely `packages/agent/docs/harness-v2.md` (`…commit-paths.txt:443-445`); its lane-reduction contract is upstream design prose, not shipped pi-agent behavior (`packages/coding-agent/package.json:83`). |
| 65 | `2901362` | docs(agent): reserve harness I0 and L1-L3 work packages | **not applicable** | Only reservation text in `packages/agent/docs/harness-v2.md` changes (`…commit-paths.txt:446-448`), which is upstream planning bookkeeping outside Atomic's pi-agent runtime dependency (`packages/coding-agent/package.json:83`). |
| 66 | `ac27947` | feat(agent): split JSONL session backend modules | **inherited dependency** | Upstream splits `packages/agent/src/harness/session/jsonl.ts` into `jsonl/{codec,errors,repo,storage,types}.ts` (`…commit-paths.txt:449-456`); it rides the resolved `pi-agent-core` package (`packages/coding-agent/package.json:83`; `package-lock.json:778-781`), not an Atomic copy. |
| 67 | `162b188` | feat(agent): add JSONL metadata contracts | **inherited dependency** | JSONL v4 metadata, header, create/list, and conformance changes are all in `packages/agent/src/harness/session/**` (`…commit-paths.txt:457-463`), delivered through `pi-agent-core` (`packages/coding-agent/package.json:83`; `package-lock.json:778-781`) rather than Atomic's `SessionManager` (`packages/coding-agent/src/core/session-manager.ts:1-37`). |
| 68 | `ea7bfdc` | docs(agent): unreserve R2 work package | **not applicable** | This removes reservation lines only from upstream `packages/agent/docs/harness-v2.md` (`…commit-paths.txt:464-466`), not from the pi-agent runtime Atomic resolves (`packages/coding-agent/package.json:83`). |
| 69 | `b780d20` | fix(tui): close truncated OSC 8 links (#7657) | **inherited dependency** | `packages/tui/src/utils.ts` now closes an active OSC 8 link after truncation (`…commit-paths.txt:467-470`); Atomic calls pi-tui `truncateToWidth` (`packages/coding-agent/src/core/tools/ask-user-question/view/components/preview/preview-box-renderer.ts:1,48`) through its resolved TUI dependency (`package-lock.json:819-822`). |
| 70 | `6bfd502` | chore: approve contributors from issue #7657 | **not applicable** | The sole path is upstream `.github/APPROVED_CONTRIBUTORS` (`…commit-paths.txt:471-473`); Atomic uses its own maintainer-assignment policy (`CONTRIBUTING.md:27-35`). |
| 71 | `c38319e` | fix(agent): share state between memory and JSONL sessions | **inherited dependency** | The shared `SessionState` projection and rewiring are confined to `packages/agent/src/harness/session/{jsonl,memory,state}.ts` (`…commit-paths.txt:474-478`) and arrive via `pi-agent-core` (`packages/coding-agent/package.json:83`; `package-lock.json:778-781`). |
| 72 | `a5953d2` | feat(agent): validate harness recovery record logs | **inherited dependency** | The new recovery-log reducer and session contracts live in `packages/agent/src/harness/{reducer.ts,session/types.ts}` (`…commit-paths.txt:479-484`), so Atomic receives them from `pi-agent-core` (`packages/coding-agent/package.json:83`; `package-lock.json:778-781`). |
| 73 | `1022a9e` | fix(agent): list SQLite sessions without writer claims (#7655) | **not applicable** | The lease-free listing behavior is in upstream `packages/session-backends/sqlite-node/**` (`…commit-paths.txt:485-498`), while the two `packages/agent` lines are `SessionRepo` documentation and Atomic resolves no session-backend package (`packages/coding-agent/package.json:80-85`; `package-lock.json:778-822`). |
| 74 | `afdf736` | docs(agent): reserve harness R1 work package | **not applicable** | Only upstream `packages/agent/docs/harness-v2.md` reservation text changes (`…commit-paths.txt:499-501`); it is not pi-agent runtime code consumed by Atomic (`packages/coding-agent/package.json:83`). |
| 75 | `e77e2b7` | fix(agent): correct JSONL fork entry semantics | **inherited dependency** | The lane-less copied-entry and explicit fork-lane fixes are in `packages/agent/src/harness/session/jsonl.ts` with its upstream test (`…commit-paths.txt:502-505`), inherited through `pi-agent-core` (`packages/coding-agent/package.json:83`; `package-lock.json:778-781`). |
| 76 | `651d5d6` | partial harness v2/json backend (#7611) | **inherited dependency** | The initial harness-v2 JSONL backend and export are in `packages/agent/src/harness/session/{index,jsonl}.ts` (`…commit-paths.txt:506-510`), a `pi-agent-core` dependency surface rather than Atomic-owned source (`packages/coding-agent/package.json:83`; `package-lock.json:778-781`). |
| 77 | `7cf90c1` | fix(coding-agent): avoid model store read lock contention | **intentionally adapted** | Upstream adds `getFileRevision` plus per-path coalesced `FileModelsStore` reloads (`…coding-agent.diff:4789-4938,8234-8241`). Atomic already has stronger lock-free auth reads, covered by `test/auth-storage-01.suite.ts:144-192`, but its model-store reads still take the exclusive lock (`src/core/models-store.ts:36-40`) and `src/utils/paths.ts:1-44` has no revision helper. Port cached/coalesced or lock-free catalog reads and concurrency coverage; retain Atomic’s deliberate in-memory identity contract (`test/models-store-snapshot.test.ts:18-28`) rather than upstream cloning. |
| 78 | `1e95e16` | fix(agent): remove redundant SQLite read clones | **not applicable** | Only `packages/session-backends/sqlite-node/src/sqlite/repo.ts` changes (`…commit-paths.txt:519-521`), and Atomic's resolved upstream packages are only agent-core, ai, and tui (`packages/coding-agent/package.json:80-85`; `package-lock.json:778-822`). |
| 79 | `591f22a` | feat(agent): add indexed harness recovery queries (#7646) | **inherited dependency** | The recovery query API touches `packages/agent/src/harness/session/{memory,session,types}.ts` alongside the SQLite backend (`…commit-paths.txt:522-532`); the agent half is delivered by `pi-agent-core` (`packages/coding-agent/package.json:83`; `package-lock.json:778-781`). |
| 80 | `f909da2` | docs(agent): reserve harness R0 work package | **not applicable** | Only `packages/agent/docs/harness-v2.md` changes (`…commit-paths.txt:533-535`); this upstream work-package note is outside the pi-agent runtime Atomic consumes (`packages/coding-agent/package.json:83`). |
| 81 | `dab70b2` | fix(server): sanitize service failures (#7644) | **not applicable** | The schema and failure sanitization changes are confined to upstream `packages/{protocol,server}/**` (`…commit-paths.txt:536-542`); Atomic declares neither package and uses its own RPC failure boundary (`packages/coding-agent/package.json:80-85`; `packages/coding-agent/src/modes/rpc/rpc-transport-error.ts:18-55`). |
| 82 | `7bdeeb8` | fix(agent): count copied messages in fork stats | **inherited dependency** | Fork-stat counting changes `packages/agent/src/harness/session/memory.ts` plus the SQLite backend (`…commit-paths.txt:543-548`); the agent portion is inherited through `pi-agent-core` (`packages/coding-agent/package.json:83`; `package-lock.json:778-781`). |
| 83 | `845bb97` | fix: build error on request info (#7645) | **intentionally adapted** | Upstream corrects the management-retry helper’s `RequestInfo` type with `Parameters<typeof fetch>[0]` and makes the version-check type predicate boolean (`git show 845bb97`; post-range `…coding-agent.diff:8152,8176-8180,8395-8412`). Atomic has neither `utils/management-http.ts`, `fetchWithRetry`, nor `formatVersionCheckError`; its branded version check and tool download calls still use direct `fetch` (`src/utils/version-check.ts:43-59`, `src/utils/tools-manager.ts:106-124`). Fold both typing corrections into the required Atomic adaptation of upstream’s management-HTTP retry feature, with its own tests. |
| 84 | `2c233a5` | fix(tui): add iterm payload size for xterm.js image addon (#7612) | **inherited dependency** | `packages/tui/src/terminal-image.ts` adds the iTerm2 decoded byte-size field (`…commit-paths.txt:553-556`); Atomic constructs pi-tui `Image` components (`packages/coding-agent/src/modes/interactive/components/tool-execution.ts:2,359-364`) from the resolved TUI package (`package-lock.json:819-822`). |
| 85 | `bb6a1cd` | refactor(server): rename backend contract to service (#7643) | **not applicable** | This is a rename across upstream `packages/server/**` (`…commit-paths.txt:557-573`), a package absent from Atomic's declared pi dependency set (`packages/coding-agent/package.json:80-85`). |
| 86 | `66534fb` | feat(coding-agent): render Mermaid diagrams (#7624) | **intentionally adapted** | Upstream adds `grok-mermaid`, a Markdown transformer, `markdown.mermaid` settings, selector wiring, and rendering tests (`…coding-agent.diff:5585-5689,6160-6248,6381-6463,6668-6680`). Atomic has no Mermaid dependency or transformer seam; `MarkdownSettings` only carries `codeBlockIndent` (`src/core/settings-types.ts:52-54`), and the split selector/mode seams are `settings-selector-{types,items,handlers}.ts:10-74,156-162,49-51` and `interactive-selectors.ts:65-165`. Port the prerequisite Markdown-transform API and Mermaid pipeline through Atomic’s split renderer/settings modules, using Atomic’s two-lock layout and new rendering/settings tests. |
| 87 | `46b53b9` | fix: retry transient management HTTP requests (#7632) | **intentionally adapted** | Upstream adds `fetchWithRetry` with shared retry timeout budget, then uses it for catalog, tool, and self-update requests (`46b53b9:packages/coding-agent/src/utils/management-http.ts`, `core/remote-catalog-provider.ts`, `utils/tools-manager.ts`, `utils/version-check.ts`). Atomic has no helper; calls remain single-shot at `core/remote-catalog-provider.ts:74-81`, `utils/tools-manager.ts:106-124`, and `utils/version-check.ts:43-51`. Its self-update seam is `src/self-update-plan.ts:25-35`, not upstream’s package CLI. Port the helper and retry/error formatting there while retaining Atomic’s npm registry URL and offline gate (`utils/version-check.ts:4,43-50`); add retry regressions beside `test/version-check.test.ts:29-107`. |
| 88 | `a80008b` | chore: rename storage package to session-backends | **not applicable** | Upstream moves `packages/storage/sqlite-node/**` to `packages/session-backends/sqlite-node/**` and adjusts root release wiring (`…commit-paths.txt:599-640`); Atomic has no such workspace or dependency (`package.json:13-15`; `packages/coding-agent/package.json:80-85`). |
| 89 | `0df5a69` | docs(agent): harness-v2: count forked messages in session stats | **not applicable** | Only upstream `packages/agent/docs/harness-v2.md` changes (`…commit-paths.txt:641-643`); it is design documentation, not a pi-agent runtime change (`packages/coding-agent/package.json:83`). |
| 90 | `464804e` | fix(tui): merge upstream main | **not applicable** | `git show --cc 464804e` has one combined-diff hunk, in `packages/tui/CHANGELOG.md`, and no runtime hunk; its TUI behavior is in the separately listed commits (`…commit-paths.txt:644`). Atomic inherits any TUI behavior through `packages/coding-agent/package.json:85`, not from merge bookkeeping. |
| 91 | `73dd066` | fix(tui): detect Windows Shift+Enter | **inherited dependency** | Windows Shift+Enter detection changes pi-tui's native modifier loader, terminal code, and Win32 prebuilds (`…commit-paths.txt:645-656`); Atomic packages that exact prebuild (`scripts/build-binaries.sh:257-263`). |
| 92 | `35074a3` | chore: approve contributors from issue #7631 | **not applicable** | The sole upstream change is `.github/APPROVED_CONTRIBUTORS` (`…commit-paths.txt:657-659`), while Atomic's contributor assignment rules are its own (`CONTRIBUTING.md:27-35`). |
| 93 | `588915e` | docs(agent): clarify JSONL fork entry replay | **not applicable** | The delta is only upstream `packages/agent/docs/harness-v2.md` (`…commit-paths.txt:660-662`), design prose excluded from the pi-agent runtime Atomic resolves (`packages/coding-agent/package.json:83`). |
| 94 | `cc4aa1a` | fix(tui): include Darwin native rebuild sources | **inherited dependency** | `packages/tui/package.json` adds Darwin rebuild sources to the published files list (`…commit-paths.txt:663-666`), so the payload change comes through Atomic's resolved pi-tui dependency (`packages/coding-agent/package.json:85`; `package-lock.json:819-822`). |
| 95 | `6a14037` | fix(agent): handle Windows resource paths | **inherited dependency** | Windows path handling changes pi-agent harness environment, prompt-template, and skill loaders (`…commit-paths.txt:667-672`), which Atomic receives from `pi-agent-core` rather than a vendored agent copy (`packages/coding-agent/package.json:83`; `package-lock.json:778-781`). |
| 96 | `7830975` | feat(tui): add Darwin native build recipe | **inherited dependency** | The Darwin native recipe and its pi-tui package script are upstream `packages/tui` changes (`…commit-paths.txt:673-677`), inherited with Atomic's resolved TUI package (`packages/coding-agent/package.json:85`; `package-lock.json:819-822`). |
| 97 | `b08e114` | docs(agent): define harness implementation packages | **not applicable** | Only `packages/agent/docs/harness-v2.md` changes (`…commit-paths.txt:678-680`); this upstream implementation plan is not part of Atomic's pi-agent runtime dependency (`packages/coding-agent/package.json:83`). |
| 98 | `fc4a3d9` | fix(coding-agent): honor output padding for errors | **intentionally adapted** | Upstream replaces `showError`’s literal padding with `this.outputPad` (`fc4a3d9:packages/coding-agent/src/modes/interactive/interactive-mode.ts`). Atomic’s split counterpart still has the pre-fix literal at `modes/interactive/interactive-editor-actions.ts:116-121`, despite live setting state at `interactive-mode-base.ts:200,363` and runtime reload at `interactive-session-runtime.ts:90-98`. Port the one call-site change while retaining Atomic’s extra trailing spacer at `interactive-editor-actions.ts:119`, then add a zero-padding error-render regression. |
| 99 | `79cc1ef` | fix(agent): own SQLite backend tests in storage package (#7626) | **not applicable** | This relocates upstream SQLite backend tests and changes a conformance test helper (`…commit-paths.txt:684-700`), while Atomic neither consumes the storage package nor its test harness (`packages/coding-agent/package.json:80-85`; `package-lock.json:778-822`). |
| 100 | `b0d382e` | fix(tui): add editor viewport key aliases (closes #7574) | **inherited dependency** | The behavior lives in upstream `packages/tui/src/keybindings.ts`: Ctrl-Home, Ctrl-End, Ctrl-PageUp, and Ctrl-PageDown become editor defaults. Atomic imports and spreads TUI defaults unchanged at `packages/coding-agent/src/core/keybindings.ts:1-8,64-65`; its resolved `@earendil-works/pi-tui` is still `0.83.0` (`package-lock.json:819-822`). A pi-tui adoption carries the aliases; Atomic’s hand-written table remains pre-alias at `packages/coding-agent/docs/keybindings.md:35-40` and must be synced then. |
| 101 | `71f6c25` | fix(ai): update Groq Qwen reasoning override | **inherited dependency** | The pi-ai model generator switches the Groq Qwen reasoning override (`…commit-paths.txt:710-714`) and runs during pi-ai's publish build, so its generated catalog reaches Atomic through `pi-ai` (`packages/coding-agent/package.json:84`; `packages/coding-agent/src/core/model-resolver-defaults.ts:19`). |
| 102 | `05bf9df` | feat: remove legacy server implementation (#7614) | **not applicable** | The removal is wholly upstream `packages/server/**` plus its root wiring (`…commit-paths.txt:715-734`); Atomic has no pi-server dependency (`packages/coding-agent/package.json:80-85`). |
| 103 | `4b85cd9` | harness-v2 mark task in progress | **not applicable** | Only a status marker in upstream `packages/agent/docs/harness-v2.md` changes (`…commit-paths.txt:735-737`), with no pi-agent runtime counterpart in Atomic (`packages/coding-agent/package.json:83`). |
| 104 | `4428955` | feat(agent): promote durable harness API | **inherited dependency** | The durable-harness promotion rewrites `packages/agent` exports and sources (`…commit-paths.txt:738-798`); Atomic's harness smoke imports come from `pi-agent-core`, not a local copy (`packages/coding-agent/test/pi-0.82.1-direct-fixes.test.ts:4-10`; `package-lock.json:778-781`). |
| 105 | `f119b01` | refactor: update sqlite for lanes (#7591) | **inherited dependency** | Alongside the upstream storage refactor, `packages/agent/src/harness/experimental/session/session.ts` exports `assertJsonSerializable` (`…commit-paths.txt:799-837`), so its agent-source change is inherited through `pi-agent-core` (`packages/coding-agent/package.json:83`; `package-lock.json:778-781`). |
| 106 | `04133eb` | docs(agent): remove superseded design docs | **not applicable** | This deletes only five upstream `packages/agent/docs/*.md` files (`…commit-paths.txt:838-844`), not source that ships through Atomic's pi-agent dependency (`packages/coding-agent/package.json:83`). |
| 107 | `4756e7d` | docs(agent): make harness v2 telemetry self-contained | **not applicable** | Only upstream `packages/agent/docs/harness-v2.md` changes (`…commit-paths.txt:845-847`); it is design prose outside Atomic's pi-agent runtime dependency (`packages/coding-agent/package.json:83`). |
| 108 | `b68dece` | docs(agent): fix coherence defects found by independent review | **not applicable** | Only upstream `packages/agent/docs/harness-v2.md` changes (`…commit-paths.txt:848-850`); documentation correction has no shipped runtime change for Atomic (`packages/coding-agent/package.json:83`). |
| 109 | `97f0ccd` | fix(coding-agent): recursively merge nested settings, closes #7572 | **intentionally adapted** | Upstream replaces the one-level object spread with a recursive merge and adds the provider-retry regression (`97f0ccd:packages/coding-agent/src/core/settings-manager.ts`). Atomic’s split helper stops after one level at `core/settings-merge.ts:4-32`, especially `:24`; it feeds global/project resolution at `settings-manager-core.ts:61-65` and Atomic’s primary-plus-legacy file merge at `settings-storage.ts:70-80`. Thus `retry.provider` (`settings-types.ts:16-27`) is still replaced wholesale. Port recursive object merging without changing array or `undefined` rules, and add the cross-scope regression missing from `test/settings-manager-provider-retry.test.ts:4-32`. |
| 110 | `d3da2e9` | fix(ai): test composite OAuth refresh cancellation | **not applicable** | The delta is solely upstream `packages/ai/test/models-runtime.test.ts` (`…commit-paths.txt:856-858`), an unpublished test-harness change; Atomic keeps separate OAuth cancellation coverage (`packages/coding-agent/test/oauth-cancellation.test.ts:23-130`). |
| 111 | `686f193` | fix(ai): separate deferred request options | **intentionally adapted** | The upstream AI type split renames `ModelsStreamTransforms` to `ModelsRequestTransforms`, separates deferred fetch/cancel options, and makes request preparation generic (`686f193:packages/ai/src/{models,types}.ts`, `packages/coding-agent/src/core/model-runtime.ts`). Atomic holds the old import, signature, and cast in its split module at `core/model-runtime-streaming.ts:13,49-52,83-90`; `ModelRuntime` only delegates normal streaming methods at `core/model-runtime.ts:392-414`. Port the generic/type migration into `ModelRuntimeStreaming` with the deferred surface from `382aa64`; the current pi-ai resolution is `0.83.0` (`package-lock.json:794-797`). |
| 112 | `f24ab6e` | fix(tui): honor nested stack minimum sizes | **inherited dependency** | The nested minimum-size fix is in `packages/tui/src/components/v-stack.ts` and its upstream layout test (`…commit-paths.txt:869-872`), inherited via Atomic's resolved pi-tui package (`packages/coding-agent/package.json:85`; `package-lock.json:819-822`). |
| 113 | `720f0e8` | fix(ai): route Copilot Grok 4.5 through Responses, closes #7560 | **inherited dependency** | Upstream changes the pi-ai model generator so Copilot `grok-4.5` uses `openai-responses` (`720f0e8:packages/ai/scripts/generate-models.ts`); its only coding-agent change is release-note text. Atomic gets built-ins solely from `@earendil-works/pi-ai/providers/all` at `core/model-runtime.ts:24,108-115`. Its Copilot layer changes only `baseUrl`, preserving model `api` at `core/copilot-env-routing.ts:91-112`, so it cannot mask the inherited catalog correction. |
| 114 | `e741cb0` | fix(coding-agent): preserve extension auth endpoints | **intentionally adapted** | Upstream adds `ResolvedRequestAuth.baseUrl` and forwards credential-resolved endpoints from `getApiKeyAndHeaders` (`e741cb0:packages/coding-agent/src/core/model-registry.ts`). Atomic omits the field at `core/model-registry.ts:6-13,69-76`, although normal runtime requests already apply it at `core/model-runtime-streaming.ts:65-73`. Atomic also has a second resolver whose surface promises `baseUrl` (`core/agent-session-methods.ts:133-135`) but whose implementation drops it (`core/agent-session-models.ts:28-35`); web summaries already wait on the missing field at `packages/web-access/web-search-summary.ts:20-26`. Port endpoint forwarding through both resolvers, keeping header-marker work scoped to row 148, then add a credential-resolved endpoint regression. This replaces `Cloudflare compatibility auth > keeps the extension facade request-auth projection intentionally base-URL-free` (`packages/coding-agent/test/model-runtime-cloudflare-compat.test.ts:35-50`): when this row lands, replace its no-`baseUrl` assertion with endpoint-forwarding coverage. |
| 115 | `42a06f9` | fix(coding-agent): test composite OAuth cancellation signal | **intentionally adapted** | Upstream’s regression verifies that extension OAuth refresh receives a composite signal whose aborted state and reason follow the caller, not the caller signal by identity (`42a06f9:packages/coding-agent/test/model-runtime-auth-options.test.ts`). Atomic lacks the underlying path: `ModelRuntimeAuthOverrides` has no signal (`core/model-runtime-types.ts:17-22`), `getAuth` accepts only those overrides (`core/model-runtime.ts:313-328`), extension `refreshToken` is one-argument (`core/extensions/provider-types.ts:37-49`), and its adapter drops a signal (`core/provider-composer-internal.ts:225-246`). Port caller cancellation through the pi-ai 0.84 auth contract and adapter, then add the composite-signal assertion beside `test/model-runtime-auth-options.test.ts:233-255`. |
| 116 | `b784c80` | fix(ai): remove OAuth refresh lock test | **not applicable** | This deletes only upstream `packages/ai/test/oauth-auth.test.ts` coverage (`…commit-paths.txt:891-893`), not a pi-ai runtime artifact Atomic receives (`packages/coding-agent/package.json:84`; `package-lock.json:794-797`). |
| 117 | `acbdc0d` | fix(ai): bound OAuth refresh duration | **inherited dependency** | The 15-second refresh bound is added in shipped `packages/ai/src/auth/resolve.ts` (`…commit-paths.txt:894-898`); Atomic's credential-print path relies on that resolver behavior (`packages/coding-agent/src/cli/credential-print.ts:432,580-583`) through pi-ai (`package-lock.json:794-797`). |
| 118 | `f7ea2ef` | docs(agent): resolve open questions, add mechanical test constructions | **not applicable** | Only upstream `packages/agent/docs/harness-v2.md` changes (`…commit-paths.txt:899-901`); these design and test-construction notes are outside Atomic's pi-agent runtime dependency (`packages/coding-agent/package.json:83`). |
| 119 | `382aa64` | DRAFT: add openai background mode responses (#7339) | **intentionally adapted** | Upstream adds deferred-response contracts in pi-ai plus `ModelRuntime.fetchDeferred`/`cancelDeferred` and provider-overlay passthrough (`382aa64:packages/ai/src/{models,types}.ts`, `packages/coding-agent/src/core/{model-runtime,provider-composer}.ts`). Atomic has no deferred symbols, and `ModelRuntime` exposes only normal delegates at `core/model-runtime.ts:392-414`; its overlay return object drops optional deferred methods at `core/provider-composer.ts:106-136`. Port public delegates and prepared deferred dispatch through Atomic’s split `core/model-runtime-streaming.ts:40-113`, preserve native-provider methods in the composer, and add the overlay regression to `test/model-runtime-modify-models-compat.test.ts:25-200`. |
| 120 | `ab5f8d8` | fix(coding-agent): retain heterogeneous auth model types | **intentionally adapted** | Upstream widens both request-auth helpers from `Model<TApi>` to `Model<any>` so an endpoint-resolved request model survives heterogeneous summary paths (`ab5f8d8:packages/coding-agent/src/core/agent-session.ts`). Atomic deliberately has one broad `Model<Api>` resolver at `core/agent-session-models.ts:10-35`, not upstream’s two-helper shape, but it returns neither a request model nor the declared `baseUrl` (`core/agent-session-methods.ts:133-135`). Its planner already supports endpoint-specific auth at `core/compaction/compaction-types.ts:80-85` and `core/compaction/range-planner.ts:168-173`. Port the invariant by forwarding resolved `baseUrl` through the single resolver and add a mixed-API compaction or summary regression; no existing Atomic test proves it. |
| 121 | `18d65de` | fix(coding-agent): preserve Copilot summary endpoint | **intentionally adapted** | Upstream threads an auth-resolved request model through summaries (`packages/coding-agent/src/core/agent-session.ts`). Atomic dispatches both paths through the session `streamFn` (`src/core/agent-session-compaction.ts:104`, `src/core/agent-session-tree.ts:136`), which applies credential `baseUrl` in `src/core/sdk.ts:252-264` and `src/core/model-runtime-streaming.ts:49-74`; however `_getRequiredRequestAuth` still drops it (`src/core/agent-session-models.ts:28-35`). Return it as `PlannerAuth.baseUrl` and add an end-to-end Copilot compaction regression. |
| 122 | `221a842` | fix(coding-agent): update undici to 8.9.0 and brace-expansion to 5.0.9 for npm audit advisories | **ported** | Upstream updates manifests and locks. Atomic carries `undici@8.9.0` in `packages/coding-agent/package.json:109` and its shrinkwrap (`npm-shrinkwrap.json:4324-4327`), plus `brace-expansion@5.0.9` through root `package.json:50` and `package-lock.json:4751-4754`; the upstream `AGENTS.md` addition is governance-only. |
| 123 | `cd20a8d` | refactor(agent): split harness experimental changes (#7587) | **inherited dependency** | Upstream changes only `packages/agent/src/harness/experimental/session/{memory.ts,testing/conformance.ts,types.ts}` (`…commit-paths.txt:929-933`); Atomic consumes `pi-agent-core` instead of vendoring it (`packages/coding-agent/package.json:83`, `package-lock.json:778-793`), so the 0.84.0 dependency bump inherits it. |
| 124 | `0524d68` | fix(coding-agent): prevent flaky RPC prompt tests | **intentionally adapted** | Upstream changes `test/rpc-prompt-response-semantics.test.ts` to use an in-memory registry. Atomic still imports and calls the disk-backed helper at `packages/coding-agent/test/rpc-prompt-response-semantics.test.ts:20,142`; it creates the shared-parent `FileModelsStore` path at `src/core/model-runtime.ts:103-107`. Port the call to existing `createInMemoryModelRegistry()` (`test/model-runtime-test-utils.ts:17-19`). |
| 125 | `1d0c974` | feat(agent): implement harness v2 for in-memory storage (#7503) | **inherited dependency** | Upstream adds `packages/agent/src/experimental.ts` and `harness/experimental/session/**` (`…commit-paths.txt:937-948`); Atomic receives `Agent` from `pi-agent-core` (`packages/coding-agent/src/core/agent-session-types.ts:1`, `package-lock.json:778-793`), so the 0.84.0 dependency bump inherits the new API. |
| 126 | `95249a7` | fix: repair tests after model refresh cancellation changes | **intentionally adapted** | Upstream repairs snapshot and caller-signal test expectations after `fed6009`. Atomic’s autocomplete fake already uses `getAvailableSnapshot` in **“matches model command arguments across provider/model order”** (`test/interactive-mode-status-autocomplete.suite.ts:335-385`), but `CreateModelRuntimeOptions` has no caller `signal` (`src/core/model-runtime-types.ts:3-15`) and package refresh supplies it only after creation (`src/package-manager-cli.ts:86-100`). Thread it through create-time restoration and add the Atomic-specific package-command regression. |
| 127 | `f27aaf6` | docs(coding-agent): document streamSimple hook contract (#7576) | **intentionally adapted** | Upstream documents the required `onPayload` and `onResponse` calls in `extensions/types.ts`. Atomic’s split public type retains only the old one-line comment (`src/core/extensions/provider-types.ts:26-27`), although custom streams receive those options (`src/core/provider-composer.ts:91-92`) and the SDK supplies the callbacks (`src/core/sdk.ts:310-339`). Port the contract to the public type and custom-provider docs. |
| 128 | `e8a1782` | fix(tui): correct terminal progress clear sequence (#7581) | **inherited dependency** | Upstream changes `packages/tui/src/terminal.ts` and its test (`…commit-paths.txt:957-960`); Atomic delegates progress writes through `this.ui.terminal.setProgress()` (`packages/coding-agent/src/modes/interactive/interactive-agent-events.ts:65-69`) and consumes `pi-tui` (`packages/coding-agent/package.json:85`), so the 0.84.0 dependency bump inherits the clear sequence. |
| 129 | `bd3440e` | chore: approve contributors from issue #7565 | **not applicable** | Upstream changes only `.github/APPROVED_CONTRIBUTORS` (`…commit-paths.txt:961-963`); Atomic's separate contributor-assignment policy is `CONTRIBUTING.md:27-35`, so this governance edit has no shipped behavior. |
| 130 | `a96fb98` | chore: approve contributors from issue #7554 | **not applicable** | Upstream changes only `.github/APPROVED_CONTRIBUTORS` (`…commit-paths.txt:964-966`); Atomic's separate contributor-assignment policy is `CONTRIBUTING.md:27-35`, so this governance edit has no shipped behavior. |
| 131 | `523b5a4` | fix(coding-agent): normalize find root results (#7569) | **intentionally adapted** | Upstream replaces prefix slicing with root-safe relativization. Atomic already uses `path.relative` and preserves directory markers (`src/core/tools/find.ts:165-169`) and resolves relative backend output against the search root (`:182-185`), but deliberately rejects filesystem-root targets (`:160-161`, `:424-425`; `test/search-tool-compatibility.test.ts:700-707`). Retain that safety policy; port the upstream root/separator helper and regressions only if root searches are enabled. |
| 132 | `25a2c8d` | Add support for generic sampling parameters (#7568) | **intentionally adapted** | Upstream adds `samplingParams` across pi-ai request serializers and coding-agent configuration. Atomic’s schemas omit it (`src/core/model-config.ts:154-187`), and composition neither accepts nor forwards it (`src/core/provider-composer-internal.ts:48-61,95-153`); resolved `pi-ai` remains `^0.83.0` (`package.json:84`). Upgrade pi-ai, then add schema, merge, model projection, docs, and request tests. |
| 133 | `fed6009` | fix(coding-agent): make model refresh cancellation caller-owned | **intentionally adapted** | Atomic has bounded refresh and generation-safe snapshots (`src/core/model-runtime.ts:457-478`, `src/core/bounded-model-refresh.ts:9-51`) plus selector cache-first rendering and close cancellation (`components/model-selector.ts:131-136,162-193`). It still refreshes before `/model` cache matching and scoped-model rendering (`interactive-model-routing.ts:51-65,144-146`), lacks caller `CreateModelRuntimeOptions.signal` (`model-runtime-types.ts:3-15`), and exposes no signal-aware credential operations (`runtime-credentials.ts:37-60`). Port those caller-owned paths and result surfaces without replacing Atomic’s bounded selector design. |
| 134 | `b06dc76` | fix(coding-agent): reinstall dependencies if `git clean` fails (#7570) | **intentionally adapted** | Upstream adds an incomplete-update marker and repairs deleted dependencies after a failed `git clean`. Atomic’s split git updater returns immediately when heads match and runs `reset` then unguarded `clean` (`src/core/package-manager-git.ts:141-169`); removal has no marker cleanup (`:186-191`). Add the marker, guarded clean, dependency repair, and clean-failure tests in this module. |
| 135 | `a0014c1` | docs(agent): finalize harness v2 on the effects variant | **not applicable** | Upstream only deletes and renames `packages/agent/docs/harness-v2*` files (`…commit-paths.txt:1078-1081`); Atomic's independently branded documentation starts at `packages/coding-agent/README.md:1`, not upstream AgentHarness docs. |
| 136 | `fa07e7b` | fix(tui): detect truecolor for Windows consoles | **inherited dependency** | Upstream changes `packages/tui/src/terminal-image.ts` (`…commit-paths.txt:1082-1085`); Atomic reaches image capabilities through `getCapabilities()` from `pi-tui` (`packages/coding-agent/src/modes/interactive/components/tool-execution.ts:2,186-187`), so the 0.84.0 dependency bump inherits the image-path fix. |
| 137 | `3d264e8` | fix(coding-agent): avoid terminal query leak on fullscreen exit | **intentionally adapted** | Upstream prevents a replacement fullscreen renderer from starting during teardown. Atomic has one `TUI` instance (`src/modes/interactive/interactive-mode-base.ts:324-328`) and shutdown only stops it (`interactive-bash-compact.ts:140-160`, `interactive-process-lifecycle.ts:66-97`), with no fullscreen renderer swap. Preserve that structural guard and add a lifecycle regression proving shutdown never restarts the terminal rather than importing upstream’s `switchUiMode` path. |
| 138 | `c72728b` | fix(coding-agent): remove obsolete --alt shortcut | **not applicable** | `git show c72728b -- packages/coding-agent/src/cli/args.ts` removes only the hidden `--alt` fullscreen alias and its test; it was added and removed within this range, so no `--alt` behavior survives in the recorded net patch. Atomic has no UI-mode parser field (`packages/coding-agent/src/cli/args.ts:35-63`), and `--alt` already follows generic unknown-long-flag handling (`:215-227`). This is in-range net-zero bookkeeping, not an architecture-based skip. |
| 139 | `da66636` | fix(coding-agent): discover sessions through symlinked directories (#7552) | **intentionally adapted** | Upstream changes the session-root scan to retain directory symlinks (`…coding-agent.diff:5550-5560`). Atomic’s split listing path still accepts only real directories at `packages/coding-agent/src/core/session-manager-list.ts:272-292`, so linked project directories disappear; `test/session-manager-list-cooperative.test.ts:103` takes the explicit-directory branch and does not cover that scan. Add the symlink predicate in this split module plus default-root linked, broken-link, and file-link regression coverage. |
| 140 | `a447534` | fix(coding-agent): make JSON streaming output linear (#7394) | **intentionally adapted** | Upstream adds the delta-only `json-event.ts` projection and applies it to print and RPC output (`…coding-agent.diff:7795-7840,7850-7888,7956-7965`). Atomic still serializes full events in `packages/coding-agent/src/modes/print-mode.ts:191-199` and `modes/rpc/rpc-session-binding.ts:141-146`; `modes/rpc/rpc-types.ts:386-387` aliases the public RPC event to the unprojected session event. Its RPC backpressure subscription does not remove cumulative snapshots. Add the projection, split public type wiring, both emit-site calls, and a linear-size regression; `test/print-mode.test.ts:209-222` checks shutdown only. |
| 141 | `32850ef` | fix(coding-agent): resume after context-limited length stops (#7540) | **intentionally adapted** | Upstream uses `isRecoverableLength` to compact-and-retry a length stop below the intended output limit, preserves the recovery budget, and uses neutral truncation copy (`…coding-agent.diff:3015,3127-3134,3276-3283,3322-3334`). Atomic instead directly continues nonzero-output below-threshold stops (`core/agent-session-auto-compaction.ts:168-180,363-371`), already drops a trailing length assistant after compaction (`:251-259`), and explicitly rejects zero-output stops in `test/agent-session-auto-compaction-queue-03.suite.ts:505-560`. Carry the pi-ai classifier and its compact-and-retry semantics through Verbatim Compaction, then update `components/assistant-message.ts:139-150` without erasing Atomic’s bounded continuation policy unless that policy is deliberately replaced. |
| 142 | `e56893f` | fix(coding-agent): prevent auto-compaction race during manual compaction (#7370) | **intentionally adapted** | Upstream keeps the agent subscription live during manual compaction by removing temporary disconnect and reconnect calls (`…coding-agent.diff:3135-3161,3210,3249-3254`). Atomic’s Verbatim Compaction split still disconnects at `core/agent-session-compaction.ts:292` and reconnects at `:300-305`; the helper remains in `core/agent-session-events.ts:496-511` and `core/agent-session-methods.ts:151-152`, so concurrent agent events can still be dropped. `test/compaction-reentrancy.test.ts:154-210` covers ownership races, not this live-response event path. Remove only the manual-compaction use while retaining disposal’s disconnect at `agent-session-events.ts:518-526`, then add the active-response regression. |
| 143 | `b103937` | feat(coding-agent): switch UI modes at runtime (#7555) | **intentionally adapted** | Upstream adds renderer replacement, overlay refusal, state and focus transfer, settings wiring, and theme-listener rebinding (`…coding-agent.diff:6602-6639,6756-6813,7192-7201,7664-7695`). Atomic constructs one `TUI` once at `packages/coding-agent/src/modes/interactive/interactive-mode-base.ts:324-329`; its settings surface has no mode entry (`interactive-selectors.ts:61-96`) and its theme controller binds one readonly TUI once (`theme/theme-controller.ts:17-34`). The pi-tui renderer primitives arrive only with the dependency update (`package.json:83-85`), while the coding-agent half must be placed across Atomic’s split interactive runtime, including safe component, overlay, focus, and listener rebinding without changing isolated-engine teardown behavior. |
| 144 | `e563301` | fix(ai): avoid stale Copilot model in overflow test | **not applicable** | Upstream changes only `packages/ai/test/context-overflow.test.ts` (`…commit-paths.txt:1143-1145`); Atomic consumes the published `pi-ai` artifact (`packages/coding-agent/npm-shrinkwrap.json:693-721`), not upstream test sources, so no runtime port is needed. |
| 145 | `305c014` | fix: make session authentication transport-specific (#7551) | **not applicable** | The behavioral change is in upstream-only `packages/{client,protocol,server}`; its sole coding-agent path is the remote-client test helper (`git show 305c014 -- packages/coding-agent/test/client/support.ts`). Atomic declares only pi-agent-core, pi-ai, and pi-tui (`packages/coding-agent/package.json:80-99`) and has no remote-session client tree. Its isolated-engine protocol is a separate local child-process protocol with no bearer field (`packages/coding-agent/src/modes/interactive-engine/protocol.ts:11,56-70`). There is no transport-auth behavior to place until Atomic adopts remote sessions themselves. |
| 146 | `0e63379` | fix(tui): handle batched color scheme reports (#7550) | **inherited dependency** | Upstream changes `packages/tui/src/terminal-colors.ts` and its test (`…commit-paths.txt:1176-1179`); Atomic calls `queryTerminalColorScheme()` through `pi-tui` (`packages/coding-agent/src/modes/interactive/theme/theme-controller.ts:109-119`), so the 0.84.0 dependency bump inherits the parser fix. |
| 147 | `c1019d9` | feat(ai): add Baseten provider | **intentionally adapted** | Baseten’s provider and request encoding live in pi-ai, but the coding-agent seams in this commit are absent from Atomic: no `baseten` thinking format or `chatTemplateArgs` schema (`core/model-config.ts:82-99`), no nested merge key (`provider-composer-internal.ts:73-92`), default model (`model-resolver-defaults.ts:4-43`), display label (`provider-display-names.ts:1-39`), or help entry (`cli/args.ts:369-400`). The provider arrives only after the pi-ai update; then Atomic must add these seams and docs. `test/unit/login-provider-parity.test.ts:76-85` will expose the missing preferred default once the provider is installed. |
| 148 | `a24fb9e` | fix(coding-agent): preserve auth header deletion markers (#7539) | **intentionally adapted** | Upstream widens the public request-auth headers to pi-ai’s nullable provider-header type and stops filtering deletion markers (`git show a24fb9e -- packages/coding-agent/src/core/model-registry.ts`). Atomic’s ModelRuntime streaming path already carries raw headers (`core/sdk.ts:253-279`), but its public `ModelRegistry` facade narrows and strips them at `core/model-registry.ts:6-13,60-76`. Preserve nullable deletion markers at that facade; `baseUrl` remains outside this header-marker port. The existing `Cloudflare compatibility auth > keeps the extension facade request-auth projection intentionally base-URL-free` test (`packages/coding-agent/test/model-runtime-cloudflare-compat.test.ts:35-50`) remains correct only until row 114 lands, when that row replaces its no-`baseUrl` assertion with endpoint-forwarding coverage. |
| 149 | `a8ee03b` | fix(tui): cache recently offscreen Kitty images | **inherited dependency** | Upstream changes `packages/tui/src/{terminal-image.ts,tui-alt-screen.ts}` (`…commit-paths.txt:1222-1228`); Atomic's image path uses `pi-tui` capabilities (`packages/coding-agent/src/modes/interactive/components/tool-execution.ts:2,186-187`), so the 0.84.0 dependency bump inherits the cache. |
| 150 | `b0e05b4` | fix(coding-agent): resize images returned by tools (#7330) | **intentionally adapted** | Upstream adds `normalizeToolResultImages`, runs it after extension `tool_result` hooks, and applies `images.autoResize` before history persistence (`…coding-agent.diff:3023,3106-3125,8272-8339`). Atomic’s split hook path has the extension rewrite and its own oversized-text redirect but no image normalizer (`core/agent-session-tool-hooks.ts:30-72`); `redirectOversizedToolResult` deliberately leaves image blocks untouched (`core/tools/oversized-tool-result.ts:219-227`). Reuse `processImage` (`utils/image-process.ts:72-119`) after the extension hook and before Atomic’s redirect, preserving its details semantics and failed-image pass-through. `test/image-resize-callers.test.ts:18-52` confirms only `read` and `@file` currently resize images. |
| 151 | `0f5286d` | feat(agent): expose shouldStopAfterTurn on Agent (#7367) | **inherited dependency** | Upstream adds `shouldStopAfterTurn` in `packages/agent/src/agent.ts` (`…commit-paths.txt:1236-1240`); Atomic imports `Agent` from `pi-agent-core` (`packages/coding-agent/src/core/agent-session-types.ts:1`), so the 0.84.0 dependency bump inherits the option. |
| 152 | `b04faa2` | fix: --model collects authed providers (#7366) | **intentionally adapted** | Upstream replaces bare exact-ID first-match selection with authenticated-match preference and an ambiguity error (`…commit-paths.txt:1241-1245`; `git show b04faa2`). Atomic still returns `findRawExactModel()`’s first `.find()` match at `packages/coding-agent/src/core/model-resolver-cli.ts:15-18,104-109`; `hasConfiguredAuth()` already exists at `src/core/model-runtime.ts:309-311`. Apply the policy across Atomic’s raw-exact routes and add ambiguous-provider cases to `packages/coding-agent/test/model-resolver.test.ts`. |
| 153 | `2e95584` | fix: validateToolArguments() coerces nullable union (#7373) | **inherited dependency** | Upstream changes `packages/ai/src/utils/validation.ts` plus its test (`…commit-paths.txt:1246-1250`); Atomic consumes `pi-ai` through its declared dependency (`packages/coding-agent/package.json:84`, `package-lock.json:794-818`), so the 0.84.0 dependency bump inherits nullable-union validation. |
| 154 | `816237c` | fix(coding-agent): target baseline x64 CPUs (#7390) | **intentionally adapted** | Upstream builds a per-platform `bun_target` and adds `-baseline` for x64 release executables (`…commit-paths.txt:1251-1254`; `git show 816237c`). Atomic compiles its split launcher with raw `--target=bun-$platform` at `scripts/build-binaries.sh:174-185` across eight targets including `linux-x64-musl` (`:164`); form a musl-inclusive x64 target while retaining Atomic’s launcher flags and Windows bytecode exception. |
| 155 | `d93e7e8` | Merge pull request #7383 from earendil-works/fix-issue-7150 | **not applicable** | `git diff-tree --cc d93e7e8` is empty; its first-parent delta is separately inventoried `8eda4f5b` (`…commits.txt:240`), not merge-only work. That source adds a manual-compaction prompt guard, which Atomic lacks between paused admission and extension interception (`packages/coding-agent/src/core/agent-session-prompt.ts:64-76`) despite its manual controller (`src/core/agent-session-compaction.ts:287-304`); account for the live change under `8eda4f5b`, not twice here. |
| 156 | `4f4762f` | feat(coding-agent): set AI_AGENT environment variable (#7493) | **intentionally adapted** | Upstream writes `AI_AGENT="pi"` in CLI and RPC entry points and documents it (`…commit-paths.txt:1256-1260`; `git show 4f4762f`). Atomic writes only its branded marker at `packages/coding-agent/src/cli.ts:16-18` and `src/rpc-entry.ts:7-9`, while release binaries start in `src/bun/split-loader.ts:6-10`; set `AI_AGENT` at the shipping seams and document it at `README.md:629-642` after choosing `atomic` or upstream’s `pi` value. |
| 157 | `cbaca60` | fix(ai): preserve Gemini 3 tool call IDs (#7494) | **inherited dependency** | Upstream changes the `pi-ai` Google adapter (`…commit-paths.txt:1261-1264`); Atomic imports its AI compatibility surface from that package (`packages/coding-agent/src/core/agent-session-types.ts:2`), so the 0.84.0 dependency bump inherits tool-call IDs. |
| 158 | `786c76c` | docs(coding-agent): fix branch summary diagram (#7526) | **intentionally adapted** | Upstream moves the branch-summary node from the abandoned branch to the target leaf (`…commit-paths.txt:1265-1267`; `git show 786c76c`). Atomic’s Verbatim Compaction code already calls `branchWithSummary(newLeafId, …)` at `packages/coding-agent/src/core/agent-session-tree.ts:157-192`, but `docs/compaction.md:335-339` still has the old diagram; replace its two ASCII lines. |
| 159 | `b9d360a` | fix(ai): retry transient provider errors in Google adapters (#7471) | **inherited dependency** | Upstream changes `pi-ai` Google adapters and adds their retry test (`…commit-paths.txt:1268-1273`); Atomic consumes `RetryCallbacks` and `RetryPolicy` from `pi-ai` (`packages/coding-agent/src/core/compaction/compaction-runner.ts:2-3`), so the 0.84.0 dependency bump inherits the retries. |
| 160 | `ebf33c0` | fix(coding-agent): flash fullscreen copy confirmation | **intentionally adapted** | Upstream gives the copy shortcut `flashConfirmation` and uses fullscreen `ui.flash("Copied!")` rather than a transcript status (`…commit-paths.txt:1274-1284`; `git show ebf33c0`). Atomic’s shortcut and `/copy` both call the zero-argument handler (`packages/coding-agent/src/modes/interactive/interactive-input-handling.ts:119-121,316-318`; `interactive-slash-commands.ts:321-334`) and only builds main-screen `TUI` (`interactive-mode-base.ts:324-329`); add the option and flash branch with fullscreen composition, retaining status for `/copy` and regular mode. |
| 161 | `c6eb628` | fix(coding-agent): bound post-login catalog refresh | **intentionally adapted** | Upstream makes `ModelRuntime.login()` await refresh with a 15-second signal combined with interaction cancellation (`…commit-paths.txt:1285-1288`; `git show c6eb628`). Atomic instead publishes the stored credential and snapshot without refreshing at `packages/coding-agent/src/core/model-runtime.ts:416-425`; UI login paths pass `modelsRefreshed: true` at `interactive-auth-login.ts:137-150,291-295`, and `/model` owns bounded catalog work at `core/bounded-model-refresh.ts:9-50`. Test: `model-runtime-refresh-bounds.test.ts:55-79`. |
| 162 | `a077fff` | fix(coding-agent): identify failed model catalogs | **intentionally adapted** | Upstream adds failed provider keys to the multi-catalog refresh message (`…commit-paths.txt:1289-1293`; `git show a077fff`). Atomic’s bounded refresh flow remains intact but emits the count-only form at `packages/coding-agent/src/modes/interactive/components/model-selector.ts:162-188,177`; `test/model-selector-refresh-status.test.ts:80-90` asserts that old text. Update the message and assertion while retaining timeout and rejection forms. |
| 163 | `f0deb8d` | Revert docs changes | **not applicable** | Upstream reverts only `packages/agent/docs/{agent-harness,harness}.md` content (`…commit-paths.txt:1294-1297`); Atomic ships its own branded coding-agent documentation (`packages/coding-agent/README.md:1`), not AgentHarness docs. |
| 164 | `01eeafd` | docs(agent): add durable queue-item cancellation to harness v2 variants | **not applicable** | Upstream changes only AgentHarness variant docs (`…commit-paths.txt:1298-1301`); Atomic ships its own branded coding-agent documentation (`packages/coding-agent/README.md:1`), not AgentHarness docs. |
| 165 | `88ed89d` | docs(agent): split harness v2 into effects and generator variants | **not applicable** | Upstream changes only AgentHarness variant docs (`…commit-paths.txt:1302-1305`); Atomic ships its own branded coding-agent documentation (`packages/coding-agent/README.md:1`), not AgentHarness docs. |
| 166 | `35fe555` | Merge pull request #7459 from earendil-works/feat/experimental-cli-composition | **not applicable** | `git diff-tree --cc 35fe555` is empty; its first-parent delta is separately inventoried `1ee411a2` experimental parser/composition work (`…commit-paths.txt:1400-1409`), not merge-only behavior. At v0.84 `experimentalCli` has no production importer, and Atomic remains a flat `parseArgs`/mode dispatcher at `packages/coding-agent/src/main.ts:201,606-670` with no `src/cli/experimental/` tree; assess an exposed CLI feature under `1ee411a2`, not twice here. |
| 167 | `4c01c70` | fix(tui): skip clipped rows during layout painting | **inherited dependency** | Upstream changes `packages/tui/src/layout.ts` and its test (`…commit-paths.txt:1307-1311`); Atomic imports transcript components from `pi-tui` (`packages/coding-agent/src/modes/interactive/components/chat-transcript.ts:1`), so the 0.84.0 dependency bump inherits layout painting. |
| 168 | `73414d0` | fix(tui): optimize alternate-screen image and layout redraws | **inherited dependency** | Runtime changes are all in `packages/tui/{src/layout.ts,src/terminal-image.ts,src/tui-alt-screen.ts}`; coding-agent changes only release-note copy (`…commit-paths.txt:1312-1321`; `git show 73414d0`). Atomic vendors no TUI source and imports pi-tui image APIs at `packages/coding-agent/src/core/tools/render-utils.ts:4,62-70` and `modes/interactive/components/tool-execution.ts:185-187`; it resolves `@earendil-works/pi-tui@0.83.0` (`package.json:85`; `package-lock.json:819-830`), so the 0.84 package update supplies the redraw fix. |
| 169 | `4279da1` | Merge pull request #7478 from earendil-works/feat/session-storage-api | **not applicable** | This is an ordinary merge with no independent combined diff (`…commit-paths.txt:1322`); its AgentHarness changes are carried by child commits `f07af61`, `a4c7cb0`, and `cbc893b`, while Atomic's agent dependency is at `package-lock.json:778-793`. |
| 170 | `e05cc6e` | docs: remove contributor changelog updates | **not applicable** | Upstream changes only `packages/{agent,storage}/**/CHANGELOG.md` (`…commit-paths.txt:1323-1326`); Atomic maintains its own `[Unreleased]` history at `packages/coding-agent/CHANGELOG.md:1-3`. |
| 171 | `f07af61` | feat(agent): compose session storage through repositories | **inherited dependency** | Upstream changes AgentHarness repository/session sources and `packages/agent/src/index.ts` (`…commit-paths.txt:1327-1356`); Atomic consumes `pi-agent-core` rather than vendoring those modules (`packages/coding-agent/package.json:83`, `package-lock.json:778-793`), so the 0.84.0 dependency bump inherits it. |
| 172 | `a4c7cb0` | feat(agent): reject unsupported session search | **inherited dependency** | Upstream changes AgentHarness `repository.ts`, `scanning-session-search.ts`, types, and exports (`…commit-paths.txt:1357-1366`); Atomic consumes `pi-agent-core` rather than vendoring those modules (`packages/coding-agent/package.json:83`, `package-lock.json:778-793`), so the 0.84.0 dependency bump inherits it. |
| 173 | `cbc893b` | feat(agent): prepare session storage foundation for harness v2 | **inherited dependency** | Upstream changes AgentHarness session-store sources and exports (`…commit-paths.txt:1367-1399`); Atomic consumes `pi-agent-core` rather than vendoring those modules (`packages/coding-agent/package.json:83`, `package-lock.json:778-793`), so the 0.84.0 dependency bump inherits it. |
| 174 | `1ee411a` | feat(coding-agent): compose experimental CLI commands | **intentionally adapted** | Upstream adds the typed `cli/experimental/{cli,command-options,command,commands/*}.ts` command tree and resolution tests (`research/upstream-v0.83.0-v0.84.0-coding-agent.diff:1915-2335,10236-10506`). Atomic has only an unrelated experimental env badge (`packages/coding-agent/src/core/experimental.ts:3-8`) and imperative command dispatch before `parseArgs` (`packages/coding-agent/src/main.ts:186-201`). This row owns the final generic command, transport, auth, and legacy-parser adapter: add it beside the current `parseArgs` call at `src/main.ts:201`, retaining Atomic branding (`src/config.ts:242-255`). Rows 181–187 record final constituent contracts of this one port, not independent Atomic surfaces. |
| 175 | `14551e7` | fix(coding-agent): increase connection attempt timeout (#7435) | **intentionally adapted** | Upstream adds `connect: { autoSelectFamilyAttemptTimeout: 2_000 }` to `EnvHttpProxyAgent` and tests the TLS-connect options (`research/upstream-v0.83.0-v0.84.0-coding-agent.diff:3972-3983,10710-10730`). Atomic’s options factory has deliberate `connectTimeout: 0` but no `connect` object (`packages/coding-agent/src/core/http-dispatcher.ts:70-82`); add the separate per-family attempt timeout and its test there without discarding Atomic’s proxy-CONNECT policy (`:114-147`). |
| 176 | `b70c0f5` | Revert "feat(tui): add switchable terminal renderers (#7440)" (#7473) | **not applicable** | This explicitly reverts `1fdf216`; the same coding-agent and TUI paths have inverse changes (`research/upstream-v0.83.0-v0.84.0-commit-paths.txt:1414-1449`). Its range net effect is nil; Atomic’s own fixed renderer construction remains at `packages/coding-agent/src/modes/interactive/interactive-mode-base.ts:324-329`. |
| 177 | `1fdf216` | feat(tui): add switchable terminal renderers (#7440) | **not applicable** | Upstream adds `SwitchableTui`, `InteractiveTui`, and live UI-mode switching, but `b70c0f5` deletes that exact feature in-range (`research/upstream-v0.83.0-v0.84.0-commit-paths.txt:1414-1449`). No switchable-renderer behavior survives to port; Atomic constructs its renderer independently at `packages/coding-agent/src/modes/interactive/interactive-mode-base.ts:324-329`. |
| 178 | `aa0ec80` | Merge pull request #7411 from earendil-works/feat/experimental-cli-parser | **not applicable** | This is an ordinary merge with no independent combined diff (`…commit-paths.txt:1450`); the experimental CLI parser behavior belongs to its child commits (`…commit-paths.txt:1451-1498`), while Atomic's current parser is `packages/coding-agent/src/cli/args.ts:1-17`. |
| 179 | `15b6617` | fix(coding-agent): preserve existing CLI option values | **intentionally adapted** | Upstream makes its experimental parser stop at the first legacy option, preserve the remainder, reject duplicate auth sources, and reject decoded-NUL Unix paths (`research/upstream-v0.83.0-v0.84.0-commit-paths.txt:1451-1455`; `git show 15b6617`). Atomic’s legacy parser takes ownership at `src/main.ts:201` and consumes its native options at `src/cli/args.ts:93-121`; the row-174 command parser must stop at its first unclaimed token and pass the untouched suffix to that exact call. Port the boundary, duplicate-auth, and decoded-NUL regressions there. |
| 180 | `b0944d7` | fix(coding-agent): validate Unix transport URLs | **intentionally adapted** | Upstream’s `transport-address.ts` rejects query, fragment, noncanonical, authority-bearing, and invalid Unix URLs (`research/upstream-v0.83.0-v0.84.0-coding-agent.diff:2336-2387`; tests at `:10358-10363`). Atomic has no transport CLI surface; add the strict `unix:///absolute/path` validator to the row-174 pre-parser immediately before `src/main.ts:201`, with those rejection regressions. |
| 181 | `c2923b6` | refactor(coding-agent): simplify experimental command names | **intentionally adapted** | Upstream renames the experimental parser’s `CliCommand` and `parseCliCommand` to `Command` and `parseCommand` (`research/upstream-v0.83.0-v0.84.0-commit-paths.txt:1460-1463`; `git show c2923b6`). Atomic has no modeled command tree at its sole parser seam (`packages/coding-agent/src/main.ts:201`), so this intermediate rename has no independent Atomic work. Use only the final `Command`/`parseCommand` names in row 174’s new layer. |
| 182 | `68ad320` | refactor(coding-agent): model experimental CLI commands | **intentionally adapted** | Upstream moves auth parsing and structured combined/server/client invocations into `cli/experimental/auth.ts` and `command.ts` (`research/upstream-v0.83.0-v0.84.0-commit-paths.txt:1464-1468`; `git show 68ad320`). Atomic has no corresponding CLI model beyond direct `parseArgs` dispatch (`packages/coding-agent/src/cli/args.ts:85-236`, `src/main.ts:201`); row 174 owns the final structured `pi`/server/client command layer, so this intermediate refactor has no separate Atomic surface. |
| 183 | `15fb81a` | refactor(coding-agent): limit experimental parser to transport options | **intentionally adapted** | Upstream narrows the experimental parser to transport/auth options and sends all residual arguments to the existing parser (`research/upstream-v0.83.0-v0.84.0-commit-paths.txt:1469-1472`; final adapter at `…coding-agent.diff:1958-1970`). Atomic has one parser only (`packages/coding-agent/src/cli/args.ts:85-236`); the row-174 adapter must parse transport/auth options and then send untouched residual tokens to the `parseArgs` call at `src/main.ts:201`. This intermediate narrowing has no other Atomic work. |
| 184 | `c95e877` | refactor(coding-agent): keep thinking validation parser-local | **equivalent** | Upstream removes `cli/thinking-level.ts`, restores validation to `cli/args.ts`, and repoints resolver imports (`research/upstream-v0.83.0-v0.84.0-commit-paths.txt:1473-1478`; `git show c95e877`). Atomic already defines and uses the validator beside `parseArgs` (`packages/coding-agent/src/cli/args.ts:66-70,157-166`) and imports it in the split resolver (`src/core/model-resolver-patterns.ts:3,151`). Passing tests: `packages/coding-agent/test/args.test.ts` › “parses --thinking” and “parses the max thinking level” (`:149-157`), plus `packages/coding-agent/test/model-resolver.test.ts` › “all valid thinking levels work” (`:128-135`) and “sonnet:random returns sonnet with undefined thinking level and warning” (`:138-145`). |
| 185 | `0ac010b` | refactor(coding-agent): rename transport endpoint to address | **intentionally adapted** | Upstream `0ac010b` renames `cli/experimental/endpoint.ts` to `transport-address.ts`, including `ExperimentalEndpoint`/`endpoint` → `TransportAddress`/`address`. Atomic routes argv straight to `parseArgs` at `src/main.ts:201`, with no experimental transport parser. This intermediate rename has no distinct Atomic work: row 174’s new layer must use the final `TransportAddress` contract at that seam, never the interim endpoint names. |
| 186 | `1ea4cca` | refactor(coding-agent): generalize experimental endpoints | **intentionally adapted** | Upstream `1ea4cca` replaces `--socket` with role-checked `--listen`/`--connect` transport URLs and separates auth parsing in `cli/experimental/{auth-options,endpoint,options}.ts`. Atomic’s `parseArgs` treats both as generic unknown flags (`packages/coding-agent/src/cli/args.ts:215-228`), so role checking must occur in the row-174 pre-parser before `src/main.ts:201`: the combined `pi` and `server` commands accept `--listen`, the `client` command accepts `--connect`, and cross-role use fails. This is a constituent of row 174, not a second parser path. |
| 187 | `f9a5c75` | feat(coding-agent): add experimental CLI option parser | **intentionally adapted** | Upstream adds the role-aware `parseExperimentalCliOptions` plus auth-option tests. Atomic retains thinking validation locally in `src/cli/args.ts:66-70,157-166`, consumed by `src/core/model-resolver-cli.ts:2,20-33`, but `src/main.ts:201` invokes only `parseArgs`; `core/experimental.ts:3-8` is an env badge, not an existing parser gate. Row 174 owns the parser and its tests; if Atomic retains an experimental gate, add it explicitly at that `main.ts` seam without moving the local validator. |
| 188 | `a6f7317` | Merge pull request #7454 from earendil-works/fix/models-json-hot-reload-test | **not applicable** | This is an ordinary merge of an upstream test-only change (`…commit-paths.txt:1499-1505`); Atomic owns its Vitest setup at `packages/coding-agent/vitest.config.ts:1-9`, so it adds no shipped behavior. |
| 189 | `960d271` | fix(coding-agent): synchronize model reload regression | **intentionally adapted** | Upstream `960d271` replaces polling with a promise resolved by the second `requestRender()`. Atomic emits initial and refreshed renders at `src/modes/interactive/components/model-selector.ts:131-187`, but its hot-reload coverage splits file reload from picker refresh and still polls with `vi.waitFor` (`test/model-registry-hot-reload.test.ts:34-79`). Port a composite render-gated regression. |
| 190 | `6374a95` | fix(coding-agent): make model reload test deterministic | **equivalent** | Upstream `6374a95` removes the shared harness in favor of a temporary `models.json`, in-memory auth, and explicit cleanup. Atomic already does this in the named test `model config hot reload > reloads the configured models.json file on every refresh` (`packages/coding-agent/test/model-registry-hot-reload.test.ts:12-15,34-50`), which creates the temp file, reloads it, and proves the old model disappears. |
| 191 | `6e48c10` | Merge pull request #7448 from earendil-works/feat/agent-branch-queries-v2 | **not applicable** | This is an ordinary merge with no independent combined diff (`…commit-paths.txt:1506`); its bounded-query changes are carried by `081d7fb` and `9b75872`, while Atomic's agent dependency is at `package-lock.json:778-793`. |
| 192 | `b5c7e55` | Merge pull request #7450 from earendil-works/fix/sqlite-compaction-discovery | **not applicable** | This is an ordinary merge with no independent combined diff (`…commit-paths.txt:1507`); its only child fix is the upstream SQLite cache change in `eb2683f`, and Atomic resolves no SQLite session backend (`package-lock.json:778-831`). |
| 193 | `eb2683f` | fix(agent): use type index for compaction discovery | **not applicable** | Upstream changes only the optional `packages/storage/sqlite-node` cache query plan (`…commit-paths.txt:1508-1510`); Atomic stores its sessions as JSONL (`packages/coding-agent/src/core/session-manager-core.ts:49-50`) and resolves no SQLite session backend (`package-lock.json:778-831`), so this SQL optimizer has no behavioral analogue. |
| 194 | `374c5b6` | Merge pull request #7449 from earendil-works/fix/agent-timeout-output-test | **not applicable** | This is an ordinary merge of the upstream agent test change recorded under `4ecd1bb` (`…commit-paths.txt:1511-1514`); Atomic owns its test configuration at `packages/coding-agent/vitest.config.ts:1-9`, so it adds no shipped behavior. |
| 195 | `4ecd1bb` | test(agent): derive timeout output size from truncation limit | **not applicable** | Upstream changes only `packages/agent/test/harness/tools.test.ts` (`…commit-paths.txt:1512-1514`); Atomic's runtime truncation limits are its own (`packages/coding-agent/src/core/tools/truncate.ts:11-13`), so no shipped source is affected. |
| 196 | `8750a38` | fix(coding-agent): make child output test deterministic | **intentionally adapted** | Upstream `8750a38` replaces live detached shells and wall-clock checks with `EventEmitter`/`PassThrough` children under fake timers, including Windows. Atomic still skips the two live-shell cases on Windows and uses `Date.now()` (`test/suite/regressions/5303-bash-output-truncation.test.ts:42-96`), though its extra hard-cap case is synthetic (`:98-132`) for its `5_000` ms cap (`src/utils/child-process.ts:17-18,137-150`). Port the deterministic first two cases while retaining that cap. |
| 197 | `081d7fb` | fix(agent): harden bounded branch queries | **inherited dependency** | Upstream changes `packages/agent/src/harness/session/array-session-reader.ts` with its optional SQLite backend (`…commit-paths.txt:1518-1524`); Atomic consumes `pi-agent-core` rather than vendoring its reader (`packages/coding-agent/package.json:83`, `package-lock.json:778-793`), so the 0.84.0 dependency bump inherits the hardening. |
| 198 | `9b75872` | feat(agent): add bounded branch queries | **inherited dependency** | Upstream adds the query surface across `packages/agent/src/harness/session/**` and `harness/types.ts` (`…commit-paths.txt:1525-1537`); Atomic consumes `pi-agent-core` rather than vendoring it (`packages/coding-agent/package.json:83`, `package-lock.json:778-793`), so the 0.84.0 dependency bump inherits it. |
| 199 | `8f9e769` | fix(coding-agent): recover stalled availability refreshes | **intentionally adapted** | Upstream `8f9e769` starts forced availability rebuilds independently and sequence-guards stale snapshots and errors in `src/core/model-runtime.ts`. Atomic’s generation guard protects a stale mutation result (`src/core/model-runtime.ts:76,179,196-212`; test `model-runtime-refresh-bounds.test.ts:122-151`), but forced refreshes still chain behind `availabilityRefresh` (`src/core/model-runtime.ts:204-226`). Port independent refresh sequencing, stale-error suppression, and the #7301 race test. |
| 200 | `583f153` | fix(tui): normalize source filenames | **inherited dependency** | Upstream `583f153` renames TUI source files to kebab case and changes one upstream test’s direct TUI-source import. Atomic vendors no `packages/tui`; it consumes `@earendil-works/pi-tui` (`packages/coding-agent/package.json:83-85`) and documents that dependency as the TUI source (`packages/coding-agent/docs/tui.md:7`). |
| 201 | `a1403af` | fix(agent): make timeout output test deterministic | **not applicable** | Upstream changes only `packages/agent/test/harness/tools.test.ts` (`…commit-paths.txt:1565-1567`); Atomic's timeout path is independently owned (`packages/coding-agent/src/core/tools/bash.ts:151-166`), so no shipped source is affected. |
| 202 | `a116523` | Merge pull request #7431 from earendil-works/refactor/sqlite-branch-cache | **not applicable** | This is an ordinary merge with no independent combined diff (`…commit-paths.txt:1568`); its SQLite cache changes are the separate `e92bf60` change, and Atomic resolves no SQLite session backend (`package-lock.json:778-831`). |
| 203 | `e92bf60` | fix(agent): make SQLite branch caching scalable | **not applicable** | Upstream changes only the optional SQLite session backend plus AgentHarness tests (`…commit-paths.txt:1569-1577`); Atomic persists sessions as JSONL (`packages/coding-agent/src/core/session-manager-core.ts:49-50`) and resolves no SQLite session backend (`package-lock.json:778-831`). |
| 204 | `4488ad5` | Merge pull request #7410 from earendil-works/fix/sqlite-session-linear-time | **not applicable** | This is an ordinary merge with no independent combined diff (`…commit-paths.txt:1578`); its SQLite/test changes are separately carried by `5758361`, and Atomic resolves no SQLite session backend (`package-lock.json:778-831`). |
| 205 | `5cd0f98` | docs(agent): clarify staged branch handling | **not applicable** | Upstream changes only an explanatory comment in `packages/storage/sqlite-node/src/sqlite/storage/index.ts` (`…commit-paths.txt:1579-1581`); Atomic's unrelated session manager is JSONL-backed (`packages/coding-agent/src/core/session-manager-core.ts:49-50`), so no behavior is ported. |
| 206 | `c2adb4d` | fix(agent): use SQLite trigger for rollback test | **not applicable** | Upstream changes only an AgentHarness SQLite migration test (`…commit-paths.txt:1582-1584`); Atomic resolves no SQLite session backend (`package-lock.json:778-831`), so no shipped behavior is affected. |
| 207 | `06a1ceb` | Merge pull request #7409 from earendil-works/feat/coding-agent-remote-client-controller | **not applicable** | This is an ordinary merge with no independent combined diff (`…commit-paths.txt:1585`); its remote-client changes belong to `a3ec51d2` (`…commit-paths.txt:1596-1625`), while Atomic resolves no upstream client or server package (`package-lock.json:778-831`). |
| 208 | `5758361` | fix(agent): make SQLite session operations linear | **not applicable** | Upstream changes only the optional SQLite session backend plus AgentHarness tests (`…commit-paths.txt:1586-1590`); Atomic persists sessions as JSONL (`packages/coding-agent/src/core/session-manager-core.ts:49-50`) and resolves no SQLite session backend (`package-lock.json:778-831`). |
| 209 | `e075116` | docs: remove contributor changelog entries | **not applicable** | Upstream `e075116` deletes contributor-only changelog lines in upstream `packages/{client,coding-agent,server}/CHANGELOG.md`. Atomic has its own release notes and `[Unreleased]` history (`packages/coding-agent/CHANGELOG.md:3-12`); no runtime behavior or shared changelog entry applies. |
| 210 | `a3ec51d` | feat: add remote session client coordination | **intentionally adapted** | Upstream `a3ec51d` adds the public `./client` export, `RemoteSession` lifecycle/transcript reducers, `pi-client`/`pi-protocol` dependencies, and client tests. Atomic has neither that export nor those dependencies (`packages/coding-agent/package.json:21-53,80-85`); its analogous coordination is child-process RPC (`src/modes/rpc/rpc-client.ts:113-185`) and isolated-engine session sync (`src/modes/interactive-engine/isolated-runtime.ts:80-109,230-279`). Port the public lifecycle and transcript contract on an Atomic RPC/engine facade, or deliberately adopt the upstream transport packages. |
| 211 | `0d43c58` | Merge pull request #7408 from earendil-works/refactor/session-reader | **not applicable** | This is an ordinary merge with no independent combined diff (`…commit-paths.txt:1626`); its reader changes are carried by `2366649`, `2cc51b9`, and `53422a2`, while Atomic's agent dependency is at `package-lock.json:778-793`. |
| 212 | `2366649` | fix(agent): avoid counting entries when opening sessions | **inherited dependency** | Upstream changes AgentHarness `array-session-reader.ts` and its public types (`…commit-paths.txt:1627-1631`); Atomic consumes `pi-agent-core` rather than vendoring them (`packages/coding-agent/package.json:83`, `package-lock.json:778-793`), so the 0.84.0 dependency bump inherits the fix. |
| 213 | `2cc51b9` | fix(agent): rename session fork module | **inherited dependency** | Upstream renames the AgentHarness fork module and updates its public index (`…commit-paths.txt:1632-1638`); Atomic consumes `pi-agent-core` rather than vendoring it (`packages/coding-agent/package.json:83`, `package-lock.json:778-793`), so the 0.84.0 dependency bump inherits it. |
| 214 | `53422a2` | feat(agent): add storage-owned session readers | **inherited dependency** | Upstream adds the AgentHarness reader and updates its session-store exports (`…commit-paths.txt:1639-1655`); Atomic consumes `pi-agent-core` rather than vendoring it (`packages/coding-agent/package.json:83`, `package-lock.json:778-793`), so the 0.84.0 dependency bump inherits it. |
| 215 | `a0bb4a4` | Merge pull request #7400 from earendil-works/refactor/sqlite-session-connection-cleanup | **not applicable** | This is an ordinary merge with no independent combined diff (`…commit-paths.txt:1656`); its integrated SQLite cleanup is upstream-only (`…commit-paths.txt:1665-1671`), while Atomic persists sessions as JSONL (`packages/coding-agent/src/core/session-manager-core.ts:49-50`). |
| 216 | `b777865` | Merge pull request #7398 from earendil-works/refactor/session-store-per-session-queues | **not applicable** | `…commit-paths.txt:1657` records only the merge and `git show --cc` has no merge-only hunk; its agent-store work is `62f3c61`, carried through Atomic’s `pi-agent-core` resolution at `package-lock.json:778-781`. |
| 217 | `03eba40` | Merge pull request #7397 from earendil-works/fix/server-protocol-invariants | **not applicable** | `…commit-paths.txt:1658` records only the merge and `git show --cc` has no merge-only hunk; its server/protocol work is `91957b3`, while Atomic declares only pi agent, AI, and TUI packages at `packages/coding-agent/package.json:83-85`. |
| 218 | `62f3c61` | feat(agent): add per-session store queues | **inherited dependency** | Upstream adds keyed per-session queues to the agent JSONL and memory stores (`…commit-paths.txt:1659-1664`); Atomic consumes `pi-agent-core` at `package-lock.json:778-781` and keeps its separate session layer at `src/core/session-manager.ts:1-11`. |
| 219 | `9f4dfdd` | fix(agent): remove obsolete SQLite session internals | **not applicable** | The patch changes one agent test and `packages/storage/sqlite-node/*` internals (`…commit-paths.txt:1665-1671`); Atomic has no storage package among its pi dependencies (`packages/coding-agent/package.json:83-85`) and owns session storage at `src/core/session-manager.ts:1-11`. |
| 220 | `91957b3` | fix(server): enforce protocol adapter invariants | **not applicable** | Upstream changes only `packages/protocol/*` and `packages/server/*` (`…commit-paths.txt:1672-1681`); Atomic declares no server or protocol package at `packages/coding-agent/package.json:83-85`. |
| 221 | `fbab971` | Merge pull request #7401 from earendil-works/fix-model-tests | **not applicable** | `…commit-paths.txt:1682` records only the merge and `git show --cc` has no merge-only hunk; its child `0c61175` changes upstream AI tests, while Atomic runs its own suite through `packages/coding-agent/package.json:76`. |
| 222 | `0c61175` | fix(ai): update Z.AI model test references | **not applicable** | All changed paths are upstream `packages/ai/test/*.test.ts` files (`…commit-paths.txt:1683-1692`); Atomic’s independent package test command is `packages/coding-agent/package.json:76`. |
| 223 | `977ec83` | Merge pull request #7391 from earendil-works/refactor/remove-session-search-index | **not applicable** | `…commit-paths.txt:1693` records only the merge and `git show --cc` has no merge-only hunk; its agent search work is classified under `9c3b271` and reaches Atomic only through `package-lock.json:778-781`. |
| 224 | `af187ee` | fix(tui): clip Kitty images to layout boundaries | **inherited dependency** | Runtime delta is only `packages/tui/src/{layout.ts,terminal-image.ts}` and its TUI test (`…commit-paths.txt:1694-1700`); coding-agent changes only its changelog. Atomic vendors no TUI and resolves `@earendil-works/pi-tui@0.83.0` (`packages/coding-agent/package.json:85`; `package-lock.json:819-822`), so the Kitty crop arrives with the pi-tui adoption. |
| 225 | `b3ed27b` | feat(coding-agent): add scrollbar thumb theme color | **intentionally adapted** | Upstream adds optional `scrollbarThumb` with a `selectedBg` fallback (`…coding-agent.diff:7704-7794`). Atomic’s split theme paths omit it: schema (`src/modes/interactive/theme/theme-schema.ts:28-39`), background union and constructor (`theme-class.ts:52-58,71-93`), and loader (`theme-loading.ts:118-135`); bundled themes lack it (`dark.json:34`, `catppuccin-mocha.json:44`). Hand-place fallback, schema, and all six bundled-theme updates, then wire the token into the fullscreen transcript composition. |
| 226 | `9c3b271` | fix(agent): make session search query-only | **inherited dependency** | Upstream removes the agent session-search index and its public export (`…commit-paths.txt:1711-1719`); Atomic consumes that package at `package-lock.json:778-781` while exporting its own `SessionManager` at `src/core/session-manager.ts:2`. |
| 227 | `7724472` | Merge pull request #7387 from earendil-works/issue/7248 | **not applicable** | Non-evil merge bookkeeping: its combined diff is empty, it has no independent inventoried path (`…commit-paths.txt:1720`), and joins `6129a35` with `bfc679d` (`…log-stat.txt:3591-3598`). The Wayland text-read behavior belongs to the separately classified `bfc679d` parent and is absent in Atomic at `src/utils/clipboard.ts:35-44`. |
| 228 | `6129a35` | feat(tui): add configurable fullscreen scrollbars | **intentionally adapted** | Upstream adds the validated `fullscreenScrollbar` setting, selector item, and live `ScrollView.setScrollbar()` wiring (`…commit-paths.txt:1721-1735`). Atomic has no setting field (`src/core/settings-types.ts:133-143`), accessor (`settings-manager-ui-accessors.ts:21-38,173-181`), selector wiring (`interactive-selectors.ts:64-96,184-187`), or transcript `ScrollView` (`interactive-mode-base.ts:312-329`). Hand-place those split seams with the fullscreen renderer rather than treating the TUI half as a skip. |
| 229 | `12a4b24` | Merge pull request #7379 from earendil-works/refactor/session-store-file-names | **not applicable** | `…commit-paths.txt:1736` records only the merge and `git show --cc` has no merge-only hunk; its agent-store changes are covered by the branch rows and Atomic’s dependency boundary is `package-lock.json:778-781`. |
| 230 | `bfc679d` | fix(coding-agent): read clipboard text on Wayland | **intentionally adapted** | Upstream prefers `wl-paste --no-newline --type text`, returns empty Wayland text as `null`, and falls back only when the command fails (`…coding-agent.diff:8117-8145`). Atomic’s reader only awaits its injectable native `source` (`src/utils/clipboard.ts:35-44`), despite owning `isWaylandSession()` (`src/utils/clipboard-image.ts:22-24`). Port the Wayland branch before that source while retaining injection; `test/clipboard.test.ts:1-207` covers writes, not this text-read route. |
| 231 | `73b2463` | Merge pull request #7386 from earendil-works/feat/server-core | **not applicable** | `…commit-paths.txt:1741` records only the merge and `git show --cc` has no merge-only hunk; its server work is `2e60d3c`, for which Atomic has no dependency at `packages/coding-agent/package.json:83-85`. |
| 232 | `546e002` | test(server): consolidate reusable test infrastructure | **not applicable** | The patch is confined to upstream server test and support files (`…commit-paths.txt:1742-1755`); Atomic has no server package in `packages/coding-agent/package.json:83-85`. |
| 233 | `15f7194` | fix(agent): scope JSONL entry IDs by path | **inherited dependency** | Upstream scopes JSONL entry-ID tracking by file path in `packages/agent/src/harness/session/jsonl-store.ts` (`…commit-paths.txt:1756-1759`); Atomic consumes the agent package through `package-lock.json:778-781` rather than vendoring that store. |
| 234 | `e36b150` | test(server): avoid Unix socket inode reuse assumption | **not applicable** | The sole changed file is upstream `packages/server/test/unix-lifecycle.test.ts` (`…commit-paths.txt:1760-1762`); Atomic has no server dependency at `packages/coding-agent/package.json:83-85`. |
| 235 | `cb86f08` | fix(server): enforce protocol adapter invariants | **not applicable** | Upstream changes `packages/server/src/protocol.ts` and its test (`…commit-paths.txt:1763-1766`); Atomic declares no upstream server or protocol package at `packages/coding-agent/package.json:83-85`. |
| 236 | `bf4a90d` | fix(tui): preserve spaces in settings searches | **inherited dependency** | The behavior is wholly in `packages/tui/src/components/settings-list.ts` and its TUI test (`…commit-paths.txt:1767-1772`). Atomic imports that `SettingsList` and enables search rather than owning its input handler (`src/modes/interactive/components/settings-selector.ts:1,21-28`); the current pi-tui resolution is `0.83.0` (`package-lock.json:819-822`). |
| 237 | `2e60d3c` | feat(server): add composable protocol server | **not applicable** | The implementation is new upstream server/protocol source and tests (`…commit-paths.txt:1773-1815`); Atomic has no `pi-server` or `pi-protocol` dependency at `packages/coding-agent/package.json:83-85`. |
| 238 | `3c71784` | feat(tui): add fullscreen transcript navigation | **inherited dependency** | Runtime changes are in `packages/tui/src/{TuiAltScreen.ts,keybindings.ts}`; coding-agent contributes only docs and changelog (`…commit-paths.txt:1816-1825`). Atomic spreads pi-tui’s binding definitions wholesale (`src/core/keybindings.ts:64-66`), so the new transcript keys arrive through the pi-tui update; no Atomic-owned binding implementation needs a copy. |
| 239 | `2b765ef` | fix(agent): restore forward session cursor reads | **inherited dependency** | Upstream makes the agent session cursor read forward from `afterEntrySeq` (`…commit-paths.txt:1826-1834`); the harness package is consumed through Atomic’s `pi-agent-core` lock entry at `package-lock.json:778-781`. |
| 240 | `8eda4f5` | fix(coding-agent): reject prompts during manual compaction | **intentionally adapted** | Upstream adds a core `_compactionAbortController` prompt guard (`…coding-agent.diff:3169-3173`). Atomic’s split prompt path has no guard (`src/core/agent-session-prompt.ts:45-76`); it instead queues ordinary interactive input (`interactive-input-handling.ts:455-464`) while manual compaction owns and clears its controller in `agent-session-compaction.ts:283-307`. Hand-place the core/RPC guard and preserve queue flushing by exposing idle state before `compaction_end`. |
| 241 | `4e81b79` | fix(agent): restrict session construction to repositories | **inherited dependency** | Upstream narrows agent session construction and exports (`…commit-paths.txt:1840-1849`); Atomic consumes that public agent package at `package-lock.json:778-781` without a vendored harness copy. |
| 242 | `f4a96c6` | docs(agent): simplify session store guidance | **not applicable** | The sole changed file is upstream agent harness documentation (`…commit-paths.txt:1850-1852`); Atomic documents its distinct JSONL session store at `packages/coding-agent/docs/sessions.md:5-24`. |
| 243 | `0b229f1` | docs(agent): document session persistence migration | **not applicable** | The sole changed file is the upstream agent changelog (`…commit-paths.txt:1853-1855`); Atomic maintains its own release history under `packages/coding-agent/CHANGELOG.md:3`. |
| 244 | `f074efd` | feat(coding-agent): add ui mode setting | **intentionally adapted** | Upstream adds the UI-mode setting, CLI flag, selector, public type, and renderer selection (`…commit-paths.txt:1856-1871`). Atomic has only output `--mode` (`src/cli/args.ts:19-64,105-109`), no renderer-mode setting (`src/core/settings-types.ts:91-144`), and one unconditional `TUI` construction (`interactive-mode-base.ts:312-329`). Port through Atomic’s split settings, args, selector, main, and renderer seams, using the range’s later `tuiMode` and `--tui-mode` names rather than obsolete `uiMode`. |
| 245 | `d937ee1` | docs(agent): remove session API migration example | **not applicable** | The sole changed file is upstream agent harness documentation (`…commit-paths.txt:1872-1874`); Atomic’s session documentation is its own `packages/coding-agent/docs/sessions.md:5-24`. |
| 246 | `f90b859` | fix(agent): update harness lifecycle tests | **not applicable** | The patch changes only upstream `packages/agent/test/harness/agent-harness.test.ts` (`…commit-paths.txt:1875-1877`); Atomic runs its independently maintained coding-agent suite via `packages/coding-agent/package.json:76`. |
| 247 | `1167980` | Merge remote-tracking branch 'origin/main' into refactor/session-store-file-names | **not applicable** | `…commit-paths.txt:1878` records a merge whose only combined-diff paths are an upstream agent test and browser-smoke script; Atomic’s runtime session layer is separate at `src/core/session-manager.ts:1-11`. |
| 248 | `2132f8f` | fix(agent): clarify session persistence ownership | **inherited dependency** | Upstream rewrites agent JSONL and memory session-store ownership (`…commit-paths.txt:1879-1913`); Atomic consumes that agent package at `package-lock.json:778-781` and retains its own session-manager API at `src/core/session-manager.ts:1-37`. |
| 249 | `696a828` | fix(tui): prevent phantom alt-screen selections | **inherited dependency** | Upstream fixes focus-loss selection state in `packages/tui/src/TuiAltScreen.ts` (`…commit-paths.txt:1914-1918`); Atomic imports its TUI components from `@earendil-works/pi-tui` at `src/modes/interactive/interactive-mode-deps.ts:18-48`. |
| 250 | `8ac92f8` | feat(tui): add draggable transient scrollbars | **intentionally adapted** | Upstream’s two coding-agent lines pass `scrollbar: "auto"` and themed scrollbar styling into the fullscreen transcript; the drag/render implementation is in pi-tui (`…commit-paths.txt:1919-1930`). Atomic’s sole interactive composition creates a plain `TUI` with no `ScrollView` (`src/modes/interactive/interactive-mode-base.ts:324-329`). Take the pi-tui behavior through its dependency update and hand-place the Atomic option wiring with the fullscreen composition. |
| 251 | `7846534` | Merge pull request #7364 from earendil-works/fix/codex-account-websocket | **not applicable** | `git show --cc 7846534` has no merge-only hunk; its AI socket behavior is `cfe6b6a`, inherited through Atomic’s `pi-ai` lock entry at `package-lock.json:794-798`. |
| 252 | `d2be68d` | fix(coding-agent): avoid auth read lock contention | **equivalent** | Upstream replaces locked reloads with shared revision/single-flight reads (`git show d2be68d`). Atomic had lock-free file reads before this range—`packages/coding-agent/src/core/auth-storage-backends.ts:22-36,93-104,130-132` reads complete atomic-renamed snapshots, while writes use temp-file plus rename (`:106-128`). The passing `AuthStorage file backend regressions > fresh storage reads credentials while another process holds the write lock` and `AuthStorage file backend regressions > many fresh storage reads succeed while the write lock is held` tests (`packages/coding-agent/test/auth-storage-01.suite.ts:144-192`) prove the non-contention behavior. |
| 253 | `7d5fc94` | Merge pull request #7371 from earendil-works/feat/unix-client-transport | **not applicable** | `git show --cc 7d5fc94` has no merge-only hunk; its child adds upstream `packages/client`, which Atomic does not declare at `packages/coding-agent/package.json:83-85`. |
| 254 | `a688e25` | fix(ai): route Fireworks Kimi K3 through OpenAI compatibility | **inherited dependency** | Upstream adds the Fireworks Kimi K3 compatibility route to the AI model generator (`…commit-paths.txt:1938-1942`); Atomic loads the pi-ai provider catalog at `src/core/model-runtime-providers.ts:1-2`. |
| 255 | `e44c129` | docs(client): document Bun Unix transport support | **not applicable** | The patch only changes upstream client documentation and `src/unix.ts` copy (`…commit-paths.txt:1943-1946`); Atomic declares no `pi-client` package at `packages/coding-agent/package.json:83-85`. |
| 256 | `1c418f6` | feat(client): add Unix socket transport | **not applicable** | Upstream adds Unix transport code exclusively under `packages/client` (`…commit-paths.txt:1947-1952`); Atomic has no upstream client dependency at `packages/coding-agent/package.json:83-85`. |
| 257 | `ea781d6` | fix: forward autocompleteMaxVisible to custom editors (#7369) | **intentionally adapted** | Upstream seeds a newly installed custom editor with `defaultEditor.getAutocompleteMaxVisible()` (`git show ea781d6`). Atomic’s split install path copies border and padding but omits this field (`packages/coding-agent/src/modes/interactive/interactive-extension-dialogs.ts:213-246`); it only synchronizes a custom editor after runtime-settings reload (`interactive-session-runtime.ts:99-106`) or selector change (`interactive-selectors.ts:203-208`). Add the guarded install-time setter after `:239-241` and a creation-time regression. |
| 258 | `cfe6b6a` | fix(ai): scope Codex sockets to accounts | **inherited dependency** | Upstream keys the Codex WebSocket cache by account in `packages/ai/src/api/openai-codex-responses.ts` (`…commit-paths.txt:1957-1960`); Atomic calls pi-ai’s Codex streamer at `src/core/codex-fast-mode.ts:1-15`. |
| 259 | `5a38a1c` | Merge pull request #7348 from earendil-works/feat/runtime-neutral-client | **not applicable** | `git show --cc 5a38a1c` has no merge-only hunk; its client work has no Atomic package analogue and Atomic declares only pi agent, AI, and TUI dependencies at `packages/coding-agent/package.json:83-85`. |
| 260 | `f9b0634` | docs(client): note promise helper removal | **not applicable** | The sole changed file is upstream `packages/client/src/promise.ts` (`…commit-paths.txt:1962-1964`); Atomic has no `pi-client` dependency at `packages/coding-agent/package.json:83-85`. |
| 261 | `6e16f77` | refactor(client): retain ES2022 compatibility | **not applicable** | Upstream changes only `packages/client/*` plus its root TypeScript configuration (`…commit-paths.txt:1965-1970`); Atomic’s separate ES2022 build target is `tsconfig.base.json:3-5` and it declares no `pi-client` package at `packages/coding-agent/package.json:83-85`. |
| 262 | `ee31567` | refactor(client): simplify lifecycle architecture | **not applicable** | The patch rewrites only upstream client source and tests (`…commit-paths.txt:1971-1988`); Atomic has no upstream client dependency at `packages/coding-agent/package.json:83-85`. |
| 263 | `7e121c6` | refactor(client): tighten lifecycle state modeling | **inherited dependency** | Besides upstream client files, the patch makes `AgentHarness.waitForShutdown()` reject before shutdown (`…commit-paths.txt:1989-2002`); that agent behavior arrives through Atomic’s `pi-agent-core` resolution at `package-lock.json:778-781`. |
| 264 | `33bc0a7` | feat(client): add runtime-neutral session client | **inherited dependency** | Although it adds upstream `packages/client`, the patch also splits agent-harness shutdown into request and wait operations (`…commit-paths.txt:2003-2028`); the agent half is inherited through `package-lock.json:778-781`. |
| 265 | `e6fb3ec` | Merge pull request #7365 from earendil-works/fix/auth-storage-lock-tests | **not applicable** | `…commit-paths.txt:2029-2032` identifies only the merged upstream coding-agent auth-storage test change, and `git show --cc` has no merge-only hunk; Atomic owns its test command at `packages/coding-agent/package.json:76`. |
| 266 | `8ee2207` | fix(coding-agent): decouple auth lock error tests | **not applicable** | Upstream changes only `test/auth-storage.test.ts`, moving the compromised-lock assertion from `Models.getAuth` to `FileAuthStorageBackend` (`git show 8ee2207`). Atomic’s suite is independently split (`packages/coding-agent/test/auth-storage.test.ts:1-3`), retains isolated temp roots (`auth-storage-02.suite.ts:9-26`), and keeps its own OAuth retry test (`:160-208`); no shipped runtime path changes. |
| 267 | `ab366eb` | fix: custom-compaction through provider via new model runtime complete (#7325) | **intentionally adapted** | Upstream routes the custom-compaction, handoff, and Q&A examples through `ModelRegistry.complete()` (`git show ab366eb`). Atomic already has `ModelRuntime.complete()` (`packages/coding-agent/src/core/model-runtime.ts:400-406`), but its extension facade has no `complete` method (`core/model-registry.ts:20-145`) and handoff/Q&A still pair `getApiKeyAndHeaders()` with pi-ai compat `complete` (`examples/extensions/handoff.ts:82-108`, `qna.ts:80-94`). Its custom-compaction is deliberately offline Verbatim Compaction returning `compactedText` (`examples/extensions/custom-compaction.ts:8-18`); add the facade and redirect handoff/Q&A, retaining that contract and adding a matching regression. |
| 268 | `59ad3de` | fix(ai): preserve Anthropic initial stream block content (#7358) | **inherited dependency** | Upstream preserves initial Anthropic text and thinking block payloads in `packages/ai/src/api/anthropic-messages.ts` (`…commit-paths.txt:2041-2044`); Atomic streams through pi-ai at `src/core/model-runtime-streaming.ts:1-19`. |
| 269 | `9786716` | fix(coding-agent): update vulnerable brace-expansion | **ported** | Upstream raises packaged `brace-expansion` from 5.0.7 to 5.0.8 in its locks (`git show 9786716`). Atomic’s later security update carries the stronger 5.0.9 pin through the root override (`package.json:47-51`), the `npm ci` lock (`package-lock.json:4751-4757`), and published shrinkwrap (`packages/coding-agent/npm-shrinkwrap.json:1617-1623`); `scripts/generate-coding-agent-shrinkwrap.test.mjs:30-55` checks root-to-shipped lock parity. Atomic’s single-lock layout deliberately replaces upstream’s `install-lock/` tree. |
| 270 | `6138f5a` | fix(ai): keep signed empty text/thinking blocks in google history (#7362) | **inherited dependency** | Upstream retains signed empty Google text and thinking blocks in `packages/ai/src/api/google-shared.ts` (`…commit-paths.txt:2051-2054`); Atomic streams through pi-ai at `src/core/model-runtime-streaming.ts:1-19`. |
| 271 | `17090d4` | feat(tui): add stacked alt-screen flashes (#7361) | **inherited dependency** | Upstream adds stacked transient flashes to `packages/tui/src/TuiAltScreen.ts` (`…commit-paths.txt:2055-2059`); Atomic imports TUI rendering primitives from pi-tui at `src/modes/interactive/interactive-mode-deps.ts:18-48`. |
| 272 | `57cde86` | fix(coding-agent): reload credentials before reads | **intentionally adapted** | Upstream makes both `read()` and `list()` reload `auth.json` at every call (`git show 57cde86`). Atomic’s `AuthStorage.read()` and `list()` return cached `data` directly (`packages/coding-agent/src/core/auth-storage.ts:24-31,76-81,121-123`), while `reload()` is explicit (`:59-74`), so a long-lived session stays stale after another process writes credentials. Add a per-read single-flight refresh through the existing layered, lock-free `FileAuthStorageBackend.read()` (`core/auth-storage-backends.ts:93-104,130-132`) and cover external edits on the same storage instance. |
| 273 | `2b50976` | chore: approve contributors from issue #7319 | **not applicable** | The sole changed file is upstream `.github/APPROVED_CONTRIBUTORS` (`…commit-paths.txt:2064-2066`); Atomic has its own contributor-assignment policy at `CONTRIBUTING.md:27-35`. |
| 274 | `3276138` | fix(coding-agent): update Doom WAD download source | **intentionally adapted** | Upstream changes the source to the gzipped gamers.org WAD and checks `IWAD` before writing (`git show 3276138`). Atomic’s published example still uses the dead direct ibiblio URL and writes unverified bytes (`packages/coding-agent/examples/extensions/doom-overlay/wad-finder.ts:1-8,41-47`); examples ship in `packages/coding-agent/package.json:55-60`. Change the URL, add `gunzipSync` plus the magic check, retain the `atomic` invocation (`doom-overlay/index.ts:4-7`), and add a fetched-fixture regression. |
| 275 | `3c5a1b2` | fix(tui): reduce default mouse wheel scroll speed | **inherited dependency** | Upstream changes the default wheel speed in `packages/tui/src/TuiAltScreen.ts` (`…commit-paths.txt:2070-2073`); Atomic uses pi-tui components through `src/modes/interactive/interactive-mode-deps.ts:18-48`. |
| 276 | `e187887` | chore: approve contributors from issue #7357 | **not applicable** | The sole changed file is upstream `.github/APPROVED_CONTRIBUTORS` (`…commit-paths.txt:2074-2076`); Atomic maintains its own contributor process at `CONTRIBUTING.md:27-35`. |
| 277 | `5f10deb` | chore: approve contributors from issue #7356 | **not applicable** | The sole changed file is upstream `.github/APPROVED_CONTRIBUTORS` (`…commit-paths.txt:2077-2079`); Atomic maintains its own contributor process at `CONTRIBUTING.md:27-35`. |
| 278 | `97cfd70` | Merge pull request #7359 from earendil-works/refactor/session-repository-factory-names | **not applicable** | Merge bookkeeping has no independently listed path (`…commit-paths.txt:2080`); the concrete agent work is dependency-owned through `@earendil-works/pi-agent-core` (`packages/coding-agent/package.json:83`) and appears in child rows. |
| 279 | `4523528` | fix(ai): treat only plain objects as provider error bodies (#7205) | **inherited dependency** | Upstream changes only `packages/ai/src/utils/error-body.ts` and its test (`…commit-paths.txt:2081-2084`); Atomic resolves, but does not vendor, pi-ai (`packages/coding-agent/package.json:84`; `package-lock.json:794-797`). |
| 280 | `66cd061` | chore: approve contributors from issue #7330 | **not applicable** | `.github/APPROVED_CONTRIBUTORS` only (`…commit-paths.txt:2085-2087`); Atomic has its own contributor-assignment policy in `CONTRIBUTING.md:27-35`, not a shipped runtime surface. |
| 281 | `8eda7d5` | fix(agent): type composed session stores explicitly | **not applicable** | This is an upstream test-only type tightening in `packages/agent/test/harness/sqlite-node.test.ts` (`…commit-paths.txt:2088-2090`); Atomic tests the public dependency surface instead (`packages/coding-agent/test/pi-0.82.1-direct-fixes.test.ts:115-120`). |
| 282 | `a1bb7e4` | fix(agent): expose session stores through factories | **inherited dependency** | The changed session-store factories live in `packages/agent/src/harness/session/*` with upstream storage code (`…commit-paths.txt:2091-2104`); Atomic consumes pi-agent-core rather than copying those modules (`packages/coding-agent/package.json:83`; `package-lock.json:778-791`). |
| 283 | `ea1e77e` | feat(tui): add alternate-screen viewport layouts | **intentionally adapted** | Upstream’s coding-agent half composes a `ScrollView` transcript and fixed `VStack` dock when alternate TUI is selected (`git show ea1e77e`). Atomic resolves pi-tui 0.83.0 (`package-lock.json:819-830`), whose exports omit the alternate-screen/layout surface (`node_modules/@earendil-works/pi-tui/dist/index.d.ts:1-23`); its host creates one main `TUI` (`src/modes/interactive/interactive-mode-base.ts:324-329`) and mounts flat regions (`interactive-startup.ts:131-150`). Bring the viewport composition with pi-tui 0.84 through the host/isolated-engine split and add an alternate-viewport regression without changing engine teardown. |
| 284 | `471c339` | chore: approve contributors from issue #7299 | **not applicable** | `.github/APPROVED_CONTRIBUTORS` only (`…commit-paths.txt:2126-2128`); Atomic’s repository process is separately defined in `CONTRIBUTING.md:27-35`. |
| 285 | `2700511` | fix(agent): refine session repository API | **inherited dependency** | The repository API change is in upstream agent/storage sources (`…commit-paths.txt:2129-2141`); Atomic’s own JSONL manager is `SessionManager` (`packages/coding-agent/src/core/session-manager-core.ts:49-50`) while pi-agent-core remains a dependency (`packages/coding-agent/package.json:83`). |
| 286 | `9b50b04` | Merge pull request #7163 from cristinaponcela/feat/search | **not applicable** | Merge bookkeeping has no independently listed path (`…commit-paths.txt:2142`); its concrete agent session-search changes are covered by constituent dependency rows, with pi-agent-core declared at `packages/coding-agent/package.json:83`. |
| 287 | `aa784e5` | refactor, cleanup and conditionals for fts5 | **inherited dependency** | FTS5/session-search changes are confined to upstream agent and SQLite backend sources (`…commit-paths.txt:2143-2153`); Atomic vendors neither and resolves pi-agent-core at `packages/coding-agent/package.json:83`. |
| 288 | `c984b7c` | cleanup | **inherited dependency** | The only production path is upstream `packages/agent/src/harness/session/session.ts` (`…commit-paths.txt:2154-2157`); Atomic receives that package through `@earendil-works/pi-agent-core` (`packages/coding-agent/package.json:83`; `package-lock.json:778-791`). |
| 289 | `fb0f0f7` | rename: vars | **inherited dependency** | Variable renames remain inside upstream agent session modules and tests (`…commit-paths.txt:2158-2165`); Atomic has no fork of those modules and consumes pi-agent-core (`packages/coding-agent/package.json:83`). |
| 290 | `74caa26` | fix(coding-agent): validate package manifests | **intentionally adapted** | Upstream adds a shared reader that accepts only object manifests and string resource arrays, preventing malformed package metadata from crashing startup (`git show 74caa26`). Atomic casts dual `atomic`/legacy `pi` manifests without field validation (`packages/coding-agent/src/core/package-manager-manifest.ts:6-15`; `core/extensions/loader-discovery.ts:16-24,51-62`), then passes entries to string-only pattern work (`package-manager-resource-collector.ts:65-76,150-171`); its schema also adds `workflows`/`workflow` (`package-manager-types.ts:145-156`). Add shared validation at the package and extension readers and a malformed-manifest regression while preserving both keys and the workflow alias. |
| 291 | `714978b` | Markdown api (#7231) | **intentionally adapted** | Upstream adds `registerMarkdownTransformer`, ordered collection, and a width-aware transform chain for user, assistant, and thinking Markdown (`git show 714978b`). Atomic has no transformer API: `ExtensionAPI` stops at message/entry renderers (`packages/coding-agent/src/core/extensions/api-types.ts:176-184`), loaded extensions have no transformer field (`runtime-types.ts:218-231`), and the message components instantiate Markdown without a transform (`packages/coding-agent/src/modes/interactive/components/assistant-message.ts:96-128`, `user-message.ts:29-42`); pi-tui 0.83’s `MarkdownOptions` also lacks it (`node_modules/@earendil-works/pi-tui/dist/components/markdown.d.ts:43-60`). Add the types, registration, accessor, and error-isolating chain through Atomic’s split renderer paths with pi-tui 0.84, then tests. |
| 292 | `70bbe47` | feat(ai): preserve structured metadata for Bedrock provider errors (#7286) | **inherited dependency** | Upstream changes pi-ai’s Bedrock stream and test (`…commit-paths.txt:2194-2197`); Atomic registers that pi-ai Bedrock module wholesale (`packages/coding-agent/src/bun/register-bedrock.ts:1-4`) through its pi-ai dependency (`packages/coding-agent/package.json:84`). |
| 293 | `dfe47d3` | fix(tui): align grapheme widths with terminal cells (#6987) | **inherited dependency** | The width heuristic is in `packages/tui/src/utils.ts` (`…commit-paths.txt:2198-2202`); Atomic imports `visibleWidth` and `truncateToWidth` from pi-tui (`packages/coding-agent/src/modes/interactive/components/footer.ts:2,49-51`). |
| 294 | `2c30412` | fix(ai): support streams without finish reasons | **inherited dependency** | Upstream adds `compat.supportsFinishReason` and infers `stop` or `toolUse` at stream end in `packages/ai/src/{types.ts,api/openai-completions.ts}` (`…coding-agent.diff:287-288,569-570`). Atomic vendors no AI source and resolves `@earendil-works/pi-ai@0.83.0` (`package-lock.json:794-798`); its generic compat merge is `packages/coding-agent/src/core/provider-composer-internal.ts:73-92`. Add the named field to Atomic’s docs/schema, now absent at `docs/models.md:491-510`, `docs/custom-provider.md:690-710`, and `src/core/model-config.ts:72-108`, with the dependency adoption. |
| 295 | `393c344` | fix(coding-agent): replace deprecated getModel in SDK example (#7306) | **intentionally adapted** | Upstream removes the compat `getModel` import and uses `modelRuntime.getModel` (`…coding-agent.diff:1284-1291`). Atomic’s branded sample still imports and calls the deprecated helper (`packages/coding-agent/examples/sdk/02-custom-model.ts:7-14`), while its public replacement exists at `src/core/model-runtime.ts:246-248` and `src/index.ts:141`. Port that two-line change into the `@bastani/atomic` example. |
| 296 | `c066d01` | Merge pull request #7344 from earendil-works/feat/protocol-package | **not applicable** | Merge bookkeeping has no independently listed path (`…commit-paths.txt:2217`); Atomic declares no upstream protocol dependency (`packages/coding-agent/package.json:80-112`), and its constituent protocol work is separately classified. |
| 297 | `b2a1a06` | docs(protocol): add package changelog | **not applicable** | The changelog and CBOR-file changes belong solely to upstream `packages/protocol` (`…commit-paths.txt:2218-2224`); Atomic does not declare or ship `pi-protocol` (`packages/coding-agent/package.json:80-112`). |
| 298 | `9cde172` | fix(agent): preserve waitForIdle semantics | **inherited dependency** | `AgentHarness.waitForIdle` and its upstream test are in `packages/agent` (`…commit-paths.txt:2225-2228`); Atomic consumes that public dependency rather than its source (`packages/coding-agent/package.json:83`; `package-lock.json:778-791`). |
| 299 | `871a990` | refactor(agent): unify harness task tracking | **inherited dependency** | The refactor changes only upstream `packages/agent/src/harness/agent-harness.ts` (`…commit-paths.txt:2229-2231`); Atomic’s AgentHarness surface comes from pi-agent-core (`packages/coding-agent/test/pi-0.82.1-direct-fixes.test.ts:115-120`). |
| 300 | `bd2cfab` | fix(protocol): reject cyclic values and organize sources | **not applicable** | CBOR codec, schema, and protocol-test paths are all upstream-only `packages/protocol` (`…commit-paths.txt:2232-2243`); Atomic has no pi-protocol dependency (`packages/coding-agent/package.json:80-112`). |
| 301 | `bc031ae` | fix(agent): await mutations during harness shutdown | **inherited dependency** | The shutdown mutation drain is implemented and tested in upstream `AgentHarness` (`…commit-paths.txt:2244-2247`); Atomic inherits it through pi-agent-core (`packages/coding-agent/package.json:83`; `package-lock.json:778-791`). |
| 302 | `b889a0c` | fix(ai): update GPT-5.6 pricing | **inherited dependency** | Pricing changes are in pi-ai model generation (`…commit-paths.txt:2248-2251`); Atomic builds its builtin catalog from pi-ai providers (`packages/coding-agent/src/core/model-runtime.ts:108-115`) declared at `packages/coding-agent/package.json:84`. |
| 303 | `56eb685` | feat(protocol): add remote session wire protocol | **not applicable** | This creates the upstream `packages/protocol` workspace and release plumbing (`…commit-paths.txt:2252-2276`); Atomic declares no remote-session protocol package (`packages/coding-agent/package.json:80-112`). |
| 304 | `7d86e62` | Merge pull request #7343 from earendil-works/feat/agent-harness-shutdown | **not applicable** | Merge bookkeeping has no independently listed path (`…commit-paths.txt:2277`); the shutdown behavior is the dependency-owned `82c4859` child change, while pi-agent-core is declared at `packages/coding-agent/package.json:83`. |
| 305 | `82c4859` | feat(agent): add harness shutdown lifecycle | **inherited dependency** | The lifecycle is added only to upstream `AgentHarness` and its tests (`…commit-paths.txt:2278-2281`); Atomic receives AgentHarness through pi-agent-core (`packages/coding-agent/package.json:83`; `package-lock.json:778-791`). |
| 306 | `83a7f75` | refactor and cleanup | **inherited dependency** | The refactor remains in upstream harness-session and SQLite backend code (`…commit-paths.txt:2282-2292`); Atomic does not vendor it and resolves pi-agent-core (`packages/coding-agent/package.json:83`). |
| 307 | `5e48d41` | refactor: repo facade | **inherited dependency** | Repository-facade changes span upstream agent session sources and SQLite backend paths (`…commit-paths.txt:2293-2306`); Atomic’s dependency boundary is pi-agent-core (`packages/coding-agent/package.json:83`; `package-lock.json:778-791`). |
| 308 | `c13ffe1` | feat(tui): add alternate-screen renderer | **intentionally adapted** | Upstream initially adds `TuiAltScreen`/`TuiMainScreen` plus provisional `--alt` parsing and interactive wiring (`…commit-paths.txt:2315-2333`; `git show c13ffe1`). Atomic still resolves `pi-tui@0.83.0` (`package-lock.json:819-830`), exposes no renderer selector (`src/cli/args.ts:21-64,198-206`; `modes/interactive/interactive-mode-types.ts:18-42`), and directly creates legacy `TUI` (`interactive-mode-base.ts:324-329`). After adopting pi-tui, port the alt-screen renderer behind final `tuiMode`/`--tui-mode fullscreen` (`v0.84.0:packages/coding-agent/src/cli/args.ts:180-186,291`) through Atomic’s split mode files; do not ship `--alt`, which row 138 records as in-range net-zero deletion, and update help/docs and renderer selection. |
| 309 | `96eda8b` | docs(agent): agent-loop building blocks in harness v2 | **not applicable** | Only upstream `packages/agent/docs/harness-v2.md` changes (`…commit-paths.txt:2352-2354`); Atomic’s user-facing documentation is its separate CLI documentation set (`packages/coding-agent/docs/index.md:6-8`). |
| 310 | `bb226f9` | fix: redundancy in setToolsExpanded (#7313) | **intentionally adapted** | Upstream makes a same-value call return immediately (`…coding-agent.diff:7092-7096`). Atomic deliberately suppresses the status notice, but still assigns state, walks every child, and renders (`packages/coding-agent/src/modes/interactive/interactive-editor-actions.ts:69-81`). Its tests cover real transitions only (`test/interactive-mode-status-basic.suite.ts:62-82`; `test/pi-0.83.0-direct-fixes.test.ts:82-110`). Add the early return and a same-state regression test. |
| 311 | `7659e1c` | Merge pull request #7289 from earendil-works/worktree/evals-baseline | **not applicable** | Merge bookkeeping: the recorded inventory places this merge directly before its merged branch commits (`…commit-paths.txt:2359-2360`), and its concrete loader/eval behavior is classified under `32f3a97`. Atomic’s separately owned Pier-based eval entry is `evals/README.md:1-22`; this merge adds no unique behavior to port. |
| 312 | `743e159` | fix(coding-agent): remove custom eval shuffling | **not applicable** | All paths belong to upstream’s non-shipped `packages/evals` Vitest harness (`…commit-paths.txt:2360-2368`); Atomic’s separate eval tooling is the Pier/Harbor suite at `evals/README.md:1-3`. |
| 313 | `ca11823` | fix(coding-agent): streamline comparative eval ergonomics | **not applicable** | Upstream changes only its `packages/evals` harness and tests (`…commit-paths.txt:2369-2376`); Atomic does not ship that harness and uses its own Pier/Harbor eval setup (`evals/README.md:1-3`). |
| 314 | `fce7445` | fix(coding-agent): make eval comparison direction explicit | **not applicable** | Comparative-report changes are confined to upstream `packages/evals` (`…commit-paths.txt:2377-2385`); Atomic’s independent evaluation adapter lives under `evals/` (`evals/atomic_harbor.py:23-26`). |
| 315 | `6bcc29a` | chore: approve contributors from issue #7300 | **not applicable** | `.github/APPROVED_CONTRIBUTORS` only (`…commit-paths.txt:2386-2388`); Atomic uses its own contributor process (`CONTRIBUTING.md:27-35`). |
| 316 | `ac033d1` | fix(coding-agent): align eval comparison report output | **not applicable** | The summary implementation and test are upstream `packages/evals` only (`…commit-paths.txt:2389-2392`); Atomic has no `vitest-evals` report surface and maintains separate eval tooling (`evals/README.md:1-3`). |
| 317 | `29f97c2` | fix(coding-agent): simplify harness table tests | **not applicable** | This is test-only maintenance in upstream `packages/evals/test/vitest-evals` (`…commit-paths.txt:2393-2395`); Atomic does not vendor that suite (`evals/README.md:1-3`). |
| 318 | `05558a7` | fix(ai): correct OpenCode Go provider display name from 'OpenCode Zen Go' to 'OpenCode Go' | **inherited dependency** | Upstream changes only pi-ai’s `opencode-go` provider (`…commit-paths.txt:2396-2398`); Atomic’s live registry takes names from the pi-ai catalog (`packages/coding-agent/src/core/model-runtime.ts:108-115`; `core/model-registry.ts:99-100`). The separate static map is only login-parity data (`test/unit/login-provider-parity.test.ts:8-42`), not the registry path; once the catalog lands it is dead weight for this label, not proof of equivalent runtime behavior. |
| 319 | `fe1f723` | chore: approve contributors from issue #7271 | **not applicable** | `.github/APPROVED_CONTRIBUTORS` only (`…commit-paths.txt:2399-2401`); Atomic’s own contributor rules are in `CONTRIBUTING.md:27-35`. |
| 320 | `d9ae6e2` | fix(coding-agent): simplify comparative eval internals | **not applicable** | All changed files are upstream `packages/evals` internals and tests (`…commit-paths.txt:2402-2408`); Atomic’s evaluation tooling is independent Pier/Harbor code (`evals/README.md:1-3`). |
| 321 | `6a3eae8` | fix(coding-agent): normalize eval inputs before hashing | **not applicable** | The hash normalization and test exist only in upstream `packages/evals/src/vitest-evals` (`…commit-paths.txt:2409-2412`); Atomic has no matching eval harness (`evals/README.md:1-3`). |
| 322 | `56e6a8a` | fix(coding-agent): record eval sessions after each test | **not applicable** | Upstream changes only its eval-runner sources and configuration (`…commit-paths.txt:2413-2419`); Atomic’s session capture belongs to its Pier/Harbor adapter (`evals/atomic_harbor.py:23-27`). |
| 323 | `2a2e677` | fix(coding-agent): make source artifacts language neutral | **not applicable** | Artifact changes are isolated to upstream `packages/evals` sources and tests (`…commit-paths.txt:2420-2424`); Atomic does not ship that artifact writer (`evals/README.md:1-3`). |
| 324 | `3631e82` | fix(coding-agent): generalize eval source artifacts | **not applicable** | The generalized artifact records are confined to upstream `packages/evals` (`…commit-paths.txt:2425-2429`); Atomic’s independent evaluation adapter is `evals/atomic_harbor.py:23-26`. |
| 325 | `eb08aa8` | fix(coding-agent): specialize eval attachment types | **not applicable** | Only upstream eval artifact types and their test change (`…commit-paths.txt:2430-2433`); Atomic has no corresponding `vitest-evals` package (`evals/README.md:1-3`). |
| 326 | `2eed6d2` | fix(coding-agent): remove redundant artifact names module | **not applicable** | The deleted artifact-names module and consumers are upstream `packages/evals` only (`…commit-paths.txt:2434-2439`); Atomic does not vendor that eval reporter (`evals/README.md:1-3`). |
| 327 | `af00848` | fix(coding-agent): record eval artifacts with explicit tasks | **not applicable** | The task-aware artifact work stays wholly in upstream `packages/evals` (`…commit-paths.txt:2440-2451`); Atomic’s eval task runner is its separate Pier/Harbor integration (`evals/README.md:16-18`). |
| 328 | `71efc6f` | Add [Unreleased] section for next cycle | **not applicable** | Upstream changes only six changelog headings (`…commit-paths.txt:2452-2459`), with no runtime source. Atomic maintains its own release history and already has `## [Unreleased]` at `packages/coding-agent/CHANGELOG.md:3`. |
| 329 | `32f3a97` | feat(coding-agent): add comparative Pi eval harness | **intentionally adapted** | Upstream adds a source-TypeScript loader branch using `virtualModules` and `tsconfigPaths` (`…coding-agent.diff:3741,3820-3826`) for its comparative eval harness. Atomic’s loader is split: known source aliases live in `packages/coding-agent/src/core/extensions/loader-virtual-modules.ts:391-445`, while virtual modules remain for Bun and dev bundles (`:475-484,539-569`). The named test `coding-agent builtin resources > loads builtin pi package resources` loads all raw builtin extension sources without errors (`test/unit/coding-agent-builtin-workflows.test.ts:185-214`). Preserve that explicit alias/virtual-module design; add any new root alias there rather than copying upstream’s branch. |
| 330 | `6a9591a` | refactor: repo owns session and storage separately | **inherited dependency** | Agent session-repository and upstream SQLite backend sources change together (`…commit-paths.txt:2479-2493`); Atomic receives the agent portion via pi-agent-core (`packages/coding-agent/package.json:83`; `package-lock.json:778-791`). |
| 331 | `3627c01` | fix: do not require index | **inherited dependency** | Optional search-index behavior changes in upstream agent session and SQLite backend sources (`…commit-paths.txt:2494-2503`); Atomic does not fork those agent modules and resolves pi-agent-core (`packages/coding-agent/package.json:83`). |
| 332 | `d5c37c6` | refactor: composable Storage vs Search | **inherited dependency** | The storage/search split is entirely upstream agent-session and SQLite backend code (`…commit-paths.txt:2504-2514`); Atomic consumes pi-agent-core rather than vendoring that implementation (`packages/coding-agent/package.json:83`; `package-lock.json:778-791`). |
| 333 | `c63bf0e` | cleanup | **not applicable** | Its sole path is upstream `packages/storage/sqlite-node/src/sqlite/search-backend.ts` (`…commit-paths.txt:2515-2517`); Atomic declares no SQLite session backend among its pi dependencies (`packages/coding-agent/package.json:83-85`; `package-lock.json:778-831`). |
| 334 | `c2bd0ed` | fix: default sqlite search backend to fts5 | **not applicable** | The patch changes only the upstream SQLite backend plus an agent harness test (`…commit-paths.txt:2518-2521`); Atomic declares no SQLite session backend (`packages/coding-agent/package.json:83-85`) and instead stores sessions as JSONL (`src/core/session-manager-core.ts:49-50`). |
| 335 | `499dd8c` | cleanup | **inherited dependency** | Upstream cleans up `packages/agent/src/harness/session/search-backend.ts` (`…commit-paths.txt:2522-2524`); Atomic consumes rather than vendors `@earendil-works/pi-agent-core` (`packages/coding-agent/package.json:83`; `package-lock.json:778-781`), so the 0.84 dependency reconciliation inherits it. |
| 336 | `5d6237a` | fix: helper owns create | **inherited dependency** | Upstream moves session creation into the agent-harness search helper across `packages/agent/src/harness/session/{jsonl-repo,memory-repo,search-backend}.ts` (`…commit-paths.txt:2525-2530`); Atomic consumes that agent package (`packages/coding-agent/package.json:83`; `package-lock.json:778-781`) while leaving the companion SQLite adapter unconsumed. |
| 337 | `3c2b71b` | refactor: search index swappable | **inherited dependency** | Upstream makes the AgentHarness search backend swappable in `packages/agent/src/harness/session/**` and `harness/types.ts` (`…commit-paths.txt:2531-2546`); Atomic inherits the agent half through `@earendil-works/pi-agent-core` (`packages/coding-agent/package.json:83`; `package-lock.json:778-781`) and resolves no SQLite session backend. |
| 338 | `0ee1ec0` | rename: may not be index | **inherited dependency** | Upstream renames the AgentHarness search-index surface to search-backend across `packages/agent/src/harness/session/**`, `harness/types.ts`, and `src/index.ts` (`…commit-paths.txt:2547-2559`); Atomic takes the resulting package through `@earendil-works/pi-agent-core` (`packages/coding-agent/package.json:83`; `package-lock.json:778-781`) rather than vendoring it. |
| 339 | `bd8dc67` | Merge branch 'main' into feat/search | **not applicable** | `…commit-paths.txt:2560` records only the merge and `git show --cc bd8dc67` has no merge-only hunk; the branch search work is carried by `b92e5e8` and `0f5a6e4` (`…commit-paths.txt:2561-2596`) through Atomic’s `pi-agent-core` dependency (`package-lock.json:778-781`). |
| 340 | `b92e5e8` | feat: search index factory | **inherited dependency** | Upstream adds the agent-harness search-index factory and public exports in `packages/agent/src/harness/session/**`, `harness/types.ts`, and `src/index.ts` (`…commit-paths.txt:2561-2573`); Atomic consumes `@earendil-works/pi-agent-core` (`packages/coding-agent/package.json:83`; `package-lock.json:778-781`) and vendors no agent source. |
| 341 | `0f5a6e4` | feat: search index sqlite | **inherited dependency** | Upstream adds AgentHarness search contracts and session-search plumbing under `packages/agent/src/harness/**` and `packages/agent/src/index.ts` (`…commit-paths.txt:2574-2596`); Atomic obtains that agent code through `@earendil-works/pi-agent-core` (`packages/coding-agent/package.json:83`; `package-lock.json:778-781`) and does not resolve the optional SQLite backend. |

## Decisions and remaining questions

The remote boundary below is resolved. Other rows remain contingent on an explicit product or architecture choice:

- **Remote client/protocol adoption and engine choice:** resolved on `pi-0.84.1/remote-session-engine` as **coexistence**. Atomic adopts upstream `pi-client`/`pi-protocol` for the experimental `@bastani/atomic/client` entrypoint; `RemoteSession` owns that transport, lease, `SessionSnapshot`, and transcript projection. Atomic's isolated interactive engine remains the owner of its child-process JSONL RPC, rendering, custom UI, and recovery lifecycle. No bridge connects the two. `SessionMetadata` remains sufficient for durable catalog listing only; runtime consumers need an acquired `SessionSnapshot`. Revisit this boundary only if upstream adds an engine-aware/server contract with teardown semantics that preserve the isolated engine's non-blocking replacement guarantee.
- **Copilot Responses sequencing (blocks on #2204):** `720f0e8` (**inherited dependency**) routes Copilot Grok 4.5 through the `openai-responses` API. That is the exact surface where Atomic's own `src/core/openai-responses-payload-sanitizer.ts` synthesizes a 64-character `function_call.id` the server rejects with `400 invalid_request_body` (bastani-inc/atomic#2204, open). Inheriting this row increases traffic on an unfixed, unrecoverable failure: #2204 must land before or with the 0.84.0 bump, not after.
- **`AI_AGENT` value:** `4f4762f` (**intentionally adapted**) needs a human choice between the upstream value `pi` and Atomic's branded value `atomic`, then the chosen value must be set at all shipping CLI/RPC seams.
- **Pi 0.83.0 → 0.84.0 dependency reconciliation:** Approve and apply the `@earendil-works/pi-agent-core`, `@earendil-works/pi-ai`, and `@earendil-works/pi-tui` 0.84.0 resolution; add the new `@earendil-works/pi-telemetry` leaf; then regenerate `package-lock.json` and `npm-shrinkwrap.json`. The current lock resolves the three pi packages at 0.83.0 (`package-lock.json:778,794,819`), so all 106 inherited-dependency rows await this action.

## Summary

| Classification | Commits | Count |
| --- | --- | ---: |
| **ported** | — | 2 |
| **intentionally adapted** | — | 77 |
| **inherited dependency** | — | 106 |
| **equivalent** | — | 4 |
| **not applicable** | — | 152 |
| **total** | every SHA in `upstream-v0.83.0-v0.84.0-commits.txt`, exactly once | **341** |

`2 + 77 + 106 + 4 + 152 = 341`.
